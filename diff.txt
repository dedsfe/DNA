diff --git a/proposta.html b/proposta.html
index 3054b5a..9278624 100644
--- a/proposta.html
+++ b/proposta.html
@@ -14,27 +14,44 @@
   
   <style>
     :root {
-      --bg-dark: #050508;
-      --bg-card: #0c0d12;
-      --bg-card-hover: #12141c;
-      --accent-gold: #d4a87a;
-      --accent-gold-rgb: 212, 168, 122;
-      --accent-blue: #3b7cd3;
-      --accent-blue-rgb: 59, 124, 211;
-      --text-primary: #ffffff;
-      --text-secondary: #a0a5b5;
-      --text-muted: #626875;
-      --border-color: rgba(255, 255, 255, 0.05);
-      --border-hover: rgba(212, 168, 122, 0.25);
-      --border-active: rgba(212, 168, 122, 0.5);
-      --max-width: 840px;
-      --transition: all 0.35s cubic-bezier(0.16, 1, 0.3, 1);
-      --shadow-premium: 0 20px 50px rgba(0, 0, 0, 0.5);
-      --shadow-card: 0 10px 30px rgba(0, 0, 0, 0.35);
-      --shadow-hover: 0 20px 45px rgba(0, 0, 0, 0.55), 0 0 0 1px rgba(212, 168, 122, 0.2);
+      --page-bg: #f5f5f7;
+      --surface-card: #ffffff;
+      --surface-input: #f8fafc;
+      --surface-soft: rgba(255, 255, 255, 0.82);
+      --ink-primary: #0a1628;
+      --ink-secondary: rgba(10, 22, 40, 0.7);
+      --ink-muted: rgba(10, 22, 40, 0.45);
+      --accent-blue: #245ba1;
+      --accent-blue-rgb: 36, 91, 161;
+      --accent-blue-hover: #1b4983;
+      --accent-blue-soft: rgba(36, 91, 161, 0.05);
+      --accent-blue-ring: rgba(36, 91, 161, 0.12);
+      --accent-gold: #dbe7f5;
+      --accent-gold-rgb: 219, 231, 245;
+      --accent-gold-soft: rgba(36, 91, 161, 0.05);
+      --accent-gold-dark: #245ba1;
+      --surface-warm: #fafaf8;
+      --border-color: rgba(10, 22, 40, 0.08);
+      --border-strong: rgba(10, 22, 40, 0.14);
+      --success: #10b981;
+      --error: #ef4444;
       --radius-sm: 8px;
-      --radius-md: 16px;
-      --radius-lg: 24px;
+      --radius: 14px;
+      --radius-lg: 22px;
+      --radius-xl: 30px;
+      --transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
+      --shadow-sm: 0 1px 2px rgba(10, 22, 40, 0.02);
+      --shadow-md:
+        0 4px 18px rgba(10, 22, 40, 0.02),
+        0 1px 3px rgba(10, 22, 40, 0.01),
+        inset 0 1px 0 rgba(255, 255, 255, 0.65);
+      --shadow-lg:
+        0 12px 32px rgba(10, 22, 40, 0.05),
+        0 2px 8px rgba(10, 22, 40, 0.03),
+        inset 0 1px 0 rgba(255, 255, 255, 0.82);
+      --shadow-hover:
+        0 24px 48px rgba(10, 22, 40, 0.08),
+        0 4px 12px rgba(10, 22, 40, 0.04);
     }
 
     * {
@@ -48,8 +65,8 @@
     }
 
     body {
-      background-color: var(--bg-dark);
-      color: var(--text-primary);
+      background-color: var(--page-bg);
+      color: var(--ink-primary);
       font-family: 'Inter', system-ui, -apple-system, sans-serif;
       line-height: 1.6;
       -webkit-font-smoothing: antialiased;
@@ -58,77 +75,75 @@
       margin: 0;
     }
 
-    /* Elegant dark mode custom scrollbar */
     ::-webkit-scrollbar {
       width: 8px;
+      height: 8px;
     }
+
     ::-webkit-scrollbar-track {
-      background: var(--bg-dark);
+      background: transparent;
     }
+
     ::-webkit-scrollbar-thumb {
-      background: rgba(255, 255, 255, 0.1);
-      border-radius: 99px;
+      background: rgba(10, 22, 40, 0.16);
+      border-radius: 999px;
     }
+
     ::-webkit-scrollbar-thumb:hover {
-      background: var(--accent-gold);
+      background: rgba(10, 22, 40, 0.28);
     }
 
-    /* Grid pattern background */
     .bg-grid {
       position: fixed;
       inset: 0;
-      background-size: 60px 60px;
-      background-image: 
-        linear-gradient(to right, rgba(255, 255, 255, 0.015) 1px, transparent 1px),
-        linear-gradient(to bottom, rgba(255, 255, 255, 0.015) 1px, transparent 1px);
+      background-size: 50px 50px;
+      background-image:
+        linear-gradient(to right, rgba(10, 22, 40, 0.012) 1px, transparent 1px),
+        linear-gradient(to bottom, rgba(10, 22, 40, 0.012) 1px, transparent 1px);
       z-index: 0;
       pointer-events: none;
     }
 
-    /* Glowing ambient lights (extremely high performance) */
-    .bg-glow-1 {
+    .bg-glow-1,
+    .bg-glow-2 {
       position: fixed;
-      top: -15%;
-      left: 15%;
-      width: 800px;
-      height: 800px;
-      background: radial-gradient(circle, rgba(212, 168, 122, 0.02) 0%, rgba(0, 0, 0, 0) 70%);
-      z-index: 0;
       pointer-events: none;
+      z-index: 0;
+    }
+
+    .bg-glow-1 {
+      top: -18%;
+      right: -12%;
+      width: 60%;
+      height: 70%;
+      background: radial-gradient(circle, rgba(36, 91, 161, 0.035) 0%, rgba(255, 255, 255, 0) 70%);
     }
+
     .bg-glow-2 {
-      position: fixed;
-      bottom: -10%;
-      right: -10%;
-      width: 700px;
-      height: 700px;
-      background: radial-gradient(circle, rgba(59, 124, 211, 0.018) 0%, rgba(0, 0, 0, 0) 70%);
-      z-index: 0;
-      pointer-events: none;
+      left: -12%;
+      bottom: -24%;
+      width: 50%;
+      height: 55%;
+      background: radial-gradient(circle, rgba(212, 168, 122, 0.07) 0%, rgba(255, 255, 255, 0) 70%);
     }
 
-    /* Mobile-stabilized viewport container */
     .page-wrapper {
       position: relative;
       width: 100%;
       min-height: 100vh;
-      overflow-x: hidden;
       display: flex;
       flex-direction: column;
       align-items: center;
-      padding: 5rem 1.5rem 8rem 1.5rem;
+      padding: clamp(2rem, 5vw, 4rem) clamp(1rem, 4vw, 2.5rem) 5rem;
       z-index: 1;
     }
 
-    /* Layout Container */
     .container {
-      max-width: var(--max-width);
-      width: 100%;
+      width: min(100%, 1040px);
       position: relative;
       z-index: 2;
     }
 
-    /* ERROR STATE SCREEN */
     .error-screen {
       display: none;
       flex-direction: column;
@@ -136,299 +151,376 @@
       justify-content: center;
       text-align: center;
       min-height: 60vh;
-      padding: 3rem;
-      background-color: var(--bg-card);
+      padding: clamp(2rem, 5vw, 4rem);
+      background: var(--surface-card);
       border: 1px solid var(--border-color);
-      border-radius: var(--radius-lg);
-      box-shadow: var(--shadow-premium);
+      border-radius: var(--radius-xl);
+      box-shadow: var(--shadow-lg);
+      position: relative;
+      overflow: hidden;
+    }
+
+    .error-screen::before {
+      content: '';
+      position: absolute;
+      inset: 0 auto auto 0;
+      width: 100%;
+      height: 4px;
+      background: linear-gradient(90deg, var(--accent-blue), var(--accent-gold));
     }
 
     .error-title {
       font-family: 'Instrument Serif', serif;
-      font-size: 3.5rem;
+      font-size: clamp(2.8rem, 8vw, 4.2rem);
       font-style: italic;
-      color: var(--accent-gold);
+      color: var(--accent-gold-dark);
       margin-bottom: 1rem;
+      line-height: 1;
     }
 
     .error-desc {
-      color: var(--text-secondary);
+      color: var(--ink-secondary);
       max-width: 440px;
-      margin-bottom: 2.5rem;
-      font-size: 1.05rem;
+      margin-bottom: 2rem;
+      font-size: 1.02rem;
     }
 
-    /* MAIN PROPOSAL VIEW */
     .proposal-view {
-      display: none; /* Shown dynamically if URL parameter exists */
+      display: none;
+      position: relative;
+      padding: clamp(2rem, 4vw, 4rem);
+      border: 1px solid var(--border-color);
+      border-radius: var(--radius-xl);
+      overflow: hidden;
+      box-shadow: var(--shadow-lg);
+      background-color: var(--surface-card);
+      background-image:
+        linear-gradient(to right, rgba(10, 22, 40, 0.018) 1px, transparent 1px),
+        linear-gradient(to bottom, rgba(10, 22, 40, 0.018) 1px, transparent 1px),
+        radial-gradient(circle at top right, rgba(36, 91, 161, 0.06) 0%, transparent 32%),
+        linear-gradient(180deg, rgba(255, 255, 255, 0.99) 0%, rgba(248, 250, 252, 0.99) 100%);
+      background-size: 48px 48px, 48px 48px, auto, auto;
       animation: proposalFadeIn 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
     }
 
+    .proposal-view::before {
+      content: '';
+      position: absolute;
+      top: 0;
+      left: 0;
+      right: 0;
+      height: 4px;
+      background: linear-gradient(90deg, var(--accent-blue), var(--accent-gold));
+      opacity: 0.9;
+    }
+
     @keyframes proposalFadeIn {
-      from { opacity: 0; transform: translateY(30px); }
-      to { opacity: 1; transform: translateY(0); }
+      from {
+        opacity: 0;
+        transform: translateY(24px);
+      }
+      to {
+        opacity: 1;
+        transform: translateY(0);
+      }
     }
 
-    /* AGENCY HEADER BRANDING */
     .brand-header {
       display: flex;
       justify-content: space-between;
-      align-items: center;
-      margin-bottom: 5rem;
-      border-bottom: 1px solid var(--border-color);
-      padding-bottom: 2rem;
+      align-items: flex-end;
+      gap: 1rem;
+      padding-bottom: 1.5rem;
+      margin-bottom: 3rem;
+      border-bottom: 1px solid rgba(10, 22, 40, 0.07);
       position: relative;
     }
 
     .brand-logo {
-      font-size: 1.75rem;
-      font-weight: 800;
-      letter-spacing: -0.05em;
-      color: var(--text-primary);
       display: flex;
-      align-items: center;
-      gap: 0.5rem;
+      align-items: baseline;
+      gap: 0.65rem;
+      font-family: 'Instrument Serif', Georgia, serif;
+      font-style: italic;
+      font-size: clamp(2.35rem, 4vw, 3rem);
+      font-weight: 400;
+      color: var(--accent-gold-dark);
+      line-height: 1;
+      letter-spacing: -0.02em;
     }
 
     .brand-logo::after {
       content: '';
       width: 8px;
       height: 8px;
-      background-color: var(--accent-gold);
+      background: var(--accent-blue);
       border-radius: 50%;
       display: inline-block;
+      margin-bottom: 0.28rem;
+      box-shadow: 0 0 0 6px rgba(36, 91, 161, 0.08);
     }
 
     .brand-tag {
-      font-size: 0.75rem;
+      font-size: 0.74rem;
       text-transform: uppercase;
-      letter-spacing: 0.25em;
-      color: var(--text-muted);
-      font-weight: 600;
+      letter-spacing: 0.16em;
+      color: var(--ink-muted);
+      font-weight: 700;
+      white-space: nowrap;
     }
 
-    /* HERO COVER */
     .proposal-hero {
-      margin-bottom: 5rem;
+      margin-bottom: 3rem;
+      break-inside: avoid;
+      page-break-inside: avoid;
     }
 
     .proposal-meta {
       font-size: 0.8rem;
       text-transform: uppercase;
-      letter-spacing: 0.25em;
-      color: var(--accent-gold);
+      letter-spacing: 0.18em;
+      color: var(--accent-blue);
       font-weight: 700;
-      margin-bottom: 1.25rem;
+      margin-bottom: 1rem;
       display: inline-flex;
       align-items: center;
-      gap: 0.5rem;
+      gap: 0.6rem;
     }
 
     .proposal-meta::before {
       content: '';
-      width: 16px;
+      width: 18px;
       height: 1px;
-      background-color: var(--accent-gold);
+      background-color: currentColor;
     }
 
     .proposal-title {
       font-family: 'Instrument Serif', serif;
-      font-size: clamp(3rem, 7vw, 5rem);
+      font-size: clamp(3rem, 8vw, 5rem);
+      font-style: italic;
       font-weight: 400;
-      line-height: 1.05;
-      letter-spacing: -0.02em;
-      color: var(--text-primary);
-      margin-bottom: 3rem;
+      line-height: 1;
+      letter-spacing: -0.03em;
+      color: var(--ink-primary);
+      margin-bottom: 2rem;
+      max-width: 12ch;
     }
 
     .proposal-client-card {
       display: inline-flex;
       flex-direction: column;
-      gap: 0.5rem;
-      border-left: 2px solid var(--accent-gold);
-      padding-left: 1.5rem;
-      margin-top: 1rem;
+      gap: 0.4rem;
+      min-width: min(100%, 420px);
+      padding: 1.25rem 1.4rem;
+      background: var(--surface-soft);
+      border: 1px solid rgba(10, 22, 40, 0.06);
+      border-left: 4px solid var(--accent-gold);
+      border-radius: var(--radius);
+      box-shadow: var(--shadow-md);
+      backdrop-filter: blur(12px);
     }
 
     .client-label {
-      font-size: 0.75rem;
+      font-size: 0.72rem;
       text-transform: uppercase;
-      letter-spacing: 0.15em;
-      color: var(--text-muted);
-      font-weight: 600;
+      letter-spacing: 0.12em;
+      color: var(--ink-muted);
+      font-weight: 700;
     }
 
     .client-company {
-      font-size: 1.5rem;
+      font-size: clamp(1.25rem, 2vw, 1.6rem);
       font-weight: 700;
-      color: var(--text-primary);
-      letter-spacing: -0.01em;
+      color: var(--ink-primary);
+      letter-spacing: -0.02em;
     }
 
     .client-name {
-      font-size: 1rem;
-      color: var(--text-secondary);
+      font-size: 0.98rem;
+      color: var(--ink-secondary);
     }
 
-    /* METRICS / STATS GRID */
     .metrics-grid {
       display: grid;
-      grid-template-columns: repeat(3, 1fr);
-      gap: 1.25rem;
-      margin-bottom: 5rem;
+      grid-template-columns: repeat(3, minmax(0, 1fr));
+      gap: 1.1rem;
+      margin-bottom: 3.25rem;
     }
 
     .metric-card {
-      background-color: var(--bg-card);
-      border: 1px solid var(--border-color);
-      border-radius: var(--radius-md);
-      padding: 1.75rem 1.5rem;
       display: flex;
       flex-direction: column;
-      gap: 0.5rem;
+      gap: 0.55rem;
+      padding: 1.5rem;
+      border-radius: var(--radius-lg);
+      border: 1px solid var(--border-color);
+      background: rgba(255, 255, 255, 0.88);
+      box-shadow: var(--shadow-md);
       transition: var(--transition);
-      box-shadow: var(--shadow-card);
+      break-inside: avoid;
+      page-break-inside: avoid;
     }
 
     .metric-card:hover {
-      border-color: var(--border-hover);
-      transform: translateY(-4px);
+      transform: translateY(-3px);
       box-shadow: var(--shadow-hover);
     }
 
     .metric-label {
-      font-size: 0.75rem;
+      font-size: 0.72rem;
       text-transform: uppercase;
-      letter-spacing: 0.15em;
-      color: var(--text-muted);
-      font-weight: 600;
+      letter-spacing: 0.12em;
+      color: var(--ink-muted);
+      font-weight: 700;
     }
 
     .metric-value {
-      font-size: 1.4rem;
+      font-size: 1.35rem;
       font-weight: 700;
-      color: var(--text-primary);
+      color: var(--ink-primary);
+      letter-spacing: -0.02em;
     }
 
     .metric-value.highlight-gold {
-      color: var(--accent-gold);
+      color: var(--accent-gold-dark);
       font-family: 'Instrument Serif', serif;
-      font-size: 2.1rem;
+      font-size: clamp(2rem, 4vw, 2.6rem);
       font-style: italic;
       font-weight: 400;
-      line-height: 1.1;
+      line-height: 1;
     }
 
-    /* SECTION LAYOUT */
     .proposal-section {
-      margin-bottom: 5rem;
+      margin-bottom: 3.25rem;
     }
 
     .section-title {
+      display: flex;
+      align-items: center;
+      gap: 0.9rem;
+      margin-bottom: 1.5rem;
+      color: var(--accent-gold-dark);
       font-family: 'Instrument Serif', serif;
-      font-size: 2.5rem;
+      font-size: clamp(2rem, 4vw, 2.6rem);
       font-style: italic;
       font-weight: 400;
-      letter-spacing: -0.01em;
-      color: var(--accent-gold);
-      margin-bottom: 2.25rem;
-      display: flex;
-      align-items: center;
-      gap: 1rem;
+      letter-spacing: -0.02em;
+      line-height: 1;
     }
 
     .section-title::after {
-      content: "";
+      content: '';
       flex: 1;
       height: 1px;
-      background: linear-gradient(90deg, var(--border-color) 0%, transparent 100%);
+      background: linear-gradient(90deg, rgba(10, 22, 40, 0.09) 0%, transparent 100%);
     }
 
-    /* APRESENTAÇÃO INSTITUCIONAL */
-    .intro-card {
-      background-color: var(--bg-card);
+    .intro-card,
+    .financials-section,
+    .signature-section {
+      background: rgba(255, 255, 255, 0.92);
       border: 1px solid var(--border-color);
-      border-left: 3px solid var(--accent-gold);
-      border-radius: var(--radius-md);
-      padding: 2.5rem;
-      box-shadow: var(--shadow-card);
+      border-radius: var(--radius-lg);
+      box-shadow: var(--shadow-md);
+      position: relative;
+      overflow: hidden;
+    }
+
+    .intro-card::before,
+    .financials-section::before,
+    .signature-section::before {
+      content: '';
+      position: absolute;
+      top: 0;
+      left: 0;
+      right: 0;
+      height: 4px;
+      background: linear-gradient(90deg, var(--accent-blue), var(--accent-gold));
+      opacity: 0.88;
+    }
+
+    .intro-card {
+      padding: 2rem;
+      border-left: 4px solid var(--accent-gold);
     }
 
     .intro-text {
-      font-size: 1.1rem;
-      line-height: 1.75;
-      color: var(--text-secondary);
-      font-weight: 400;
+      color: var(--ink-secondary);
+      font-size: 1.05rem;
+      line-height: 1.8;
     }
 
-    /* OBJETIVOS DO PROJETO */
     .objectives-grid {
       display: grid;
-      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
-      gap: 1.5rem;
+      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
+      gap: 1rem;
     }
 
     .objective-card {
-      background-color: var(--bg-card);
-      border: 1px solid var(--border-color);
-      border-radius: var(--radius-md);
-      padding: 2rem;
       display: flex;
-      gap: 1.25rem;
       align-items: flex-start;
+      gap: 1rem;
+      padding: 1.4rem 1.3rem;
+      border-radius: var(--radius);
+      border: 1px solid var(--border-color);
+      background: rgba(255, 255, 255, 0.85);
+      box-shadow: var(--shadow-sm);
       transition: var(--transition);
-      box-shadow: var(--shadow-card);
+      break-inside: avoid;
+      page-break-inside: avoid;
     }
 
     .objective-card:hover {
-      border-color: var(--border-hover);
-      transform: translateY(-4px);
-      box-shadow: var(--shadow-hover);
+      border-color: rgba(var(--accent-blue-rgb), 0.2);
+      transform: translateY(-2px);
+      box-shadow: var(--shadow-md);
     }
 
     .objective-num-wrapper {
       display: flex;
       align-items: center;
       justify-content: center;
-      width: 40px;
-      height: 40px;
-      background-color: rgba(var(--accent-gold-rgb), 0.08);
-      border: 1px solid rgba(var(--accent-gold-rgb), 0.15);
+      width: 38px;
+      height: 38px;
+      background: var(--accent-blue-soft);
+      border: 1px solid rgba(var(--accent-blue-rgb), 0.16);
       border-radius: 50%;
       flex-shrink: 0;
     }
 
     .objective-num {
       font-family: 'Instrument Serif', serif;
-      font-size: 1.5rem;
+      font-size: 1.35rem;
       font-style: italic;
-      color: var(--accent-gold);
+      color: var(--accent-blue);
       line-height: 1;
       font-weight: 600;
     }
 
     .objective-content {
-      font-size: 1rem;
-      color: var(--text-secondary);
+      color: var(--ink-secondary);
+      font-size: 0.98rem;
       line-height: 1.65;
     }
 
-    /* SCOPE ITEMS */
     .scope-list {
       display: flex;
       flex-direction: column;
-      gap: 2.25rem;
+      gap: 1.35rem;
     }
 
     .scope-card {
-      background-color: var(--bg-card);
+      background: linear-gradient(180deg, #ffffff 0%, var(--surface-input) 100%);
       border: 1px solid var(--border-color);
+      border-left: 4px solid rgba(var(--accent-blue-rgb), 0.2);
       border-radius: var(--radius-lg);
-      padding: 2.5rem;
+      padding: 1.75rem;
       position: relative;
       transition: var(--transition);
-      box-shadow: var(--shadow-card);
+      box-shadow: var(--shadow-sm);
       overflow: hidden;
+      break-inside: avoid;
+      page-break-inside: avoid;
     }
 
     .scope-card::before {
@@ -438,37 +530,36 @@
       left: 0;
       right: 0;
       height: 2px;
-      background: linear-gradient(90deg, transparent, rgba(var(--accent-gold-rgb), 0.15), transparent);
+      background: linear-gradient(90deg, rgba(var(--accent-blue-rgb), 0.65), rgba(var(--accent-gold-rgb), 0.65));
       opacity: 0;
       transition: var(--transition);
     }
 
-    .scope-card:not(.is-deselected):hover {
-      border-color: var(--border-hover);
-      box-shadow: var(--shadow-hover);
-    }
-
     .scope-card:not(.is-deselected) {
-      border-color: rgba(var(--accent-gold-rgb), 0.15);
+      border-left-color: var(--accent-blue);
     }
 
     .scope-card:not(.is-deselected)::before {
       opacity: 1;
     }
 
+    .scope-card:not(.is-deselected):hover {
+      transform: translateY(-3px);
+      box-shadow: var(--shadow-md);
+    }
+
     .scope-card.is-deselected {
-      opacity: 0.35;
-      filter: grayscale(85%);
-      border-color: rgba(255, 255, 255, 0.02);
-      transform: scale(0.99);
+      opacity: 0.45;
+      filter: grayscale(0.35);
+      border-left-color: rgba(10, 22, 40, 0.14);
       box-shadow: none;
     }
 
-    /* PREMIUM CHECKBOX STYLING */
     .scope-select-wrap {
       display: flex;
       align-items: center;
-      gap: 1.5rem;
+      gap: 1rem;
+      min-width: 0;
     }
 
     .premium-checkbox-container {
@@ -478,6 +569,7 @@
       height: 28px;
       cursor: pointer;
       user-select: none;
+      flex-shrink: 0;
     }
 
     .premium-checkbox-container input {
@@ -490,268 +582,241 @@
 
     .premium-checkbox-checkmark {
       position: absolute;
-      top: 0;
-      left: 0;
-      height: 28px;
-      width: 28px;
-      background-color: transparent;
-      border: 2px solid rgba(var(--accent-gold-rgb), 0.35);
+      inset: 0;
+      border: 2px solid rgba(var(--accent-blue-rgb), 0.28);
+      background: rgba(255, 255, 255, 0.9);
       border-radius: 8px;
       transition: var(--transition);
+      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.8);
     }
 
     .premium-checkbox-container:hover input ~ .premium-checkbox-checkmark {
-      border-color: var(--accent-gold);
-      background-color: rgba(var(--accent-gold-rgb), 0.05);
+      border-color: var(--accent-blue);
+      background: var(--accent-blue-soft);
     }
 
     .premium-checkbox-container input:checked ~ .premium-checkbox-checkmark {
-      background-color: var(--accent-gold);
-      border-color: var(--accent-gold);
-      box-shadow: 0 0 15px rgba(var(--accent-gold-rgb), 0.4);
+      background: var(--accent-blue);
+      border-color: var(--accent-blue);
+      box-shadow: 0 10px 22px rgba(var(--accent-blue-rgb), 0.22);
     }
 
-    .premium-checkbox-checkmark:after {
-      content: "";
+    .premium-checkbox-checkmark::after {
+      content: '';
       position: absolute;
       display: none;
-    }
-
-    .premium-checkbox-container input:checked ~ .premium-checkbox-checkmark:after {
-      display: block;
-    }
-
-    .premium-checkbox-container .premium-checkbox-checkmark:after {
       left: 9px;
       top: 4px;
       width: 6px;
       height: 12px;
-      border: solid var(--bg-dark);
+      border: solid #ffffff;
       border-width: 0 3px 3px 0;
       transform: rotate(45deg);
     }
 
+    .premium-checkbox-container input:checked ~ .premium-checkbox-checkmark::after {
+      display: block;
+    }
+
     .scope-header {
       display: flex;
       justify-content: space-between;
       align-items: flex-start;
-      margin-bottom: 1.75rem;
-      gap: 1.25rem;
+      gap: 1rem;
+      margin-bottom: 1.25rem;
+      padding-bottom: 1rem;
+      border-bottom: 1px dashed rgba(10, 22, 40, 0.12);
     }
 
     .scope-number-title {
       display: flex;
       flex-direction: column;
       gap: 0.35rem;
+      min-width: 0;
     }
 
     .scope-num {
-      font-size: 0.75rem;
-      font-family: monospace;
-      color: var(--accent-gold);
+      font-size: 0.74rem;
+      color: var(--accent-blue);
       font-weight: 700;
-      letter-spacing: 0.1em;
+      letter-spacing: 0.12em;
+      text-transform: uppercase;
     }
 
     .scope-title {
-      font-size: 1.45rem;
+      font-size: clamp(1.2rem, 2vw, 1.45rem);
       font-weight: 700;
-      color: var(--text-primary);
+      color: var(--ink-primary);
       letter-spacing: -0.02em;
     }
 
     .scope-price-badge {
-      padding: 0.45rem 1rem;
-      background-color: rgba(var(--accent-gold-rgb), 0.08);
-      border: 1px solid rgba(var(--accent-gold-rgb), 0.2);
-      color: var(--accent-gold);
-      border-radius: 99px;
-      font-size: 0.85rem;
+      padding: 0.5rem 0.9rem;
+      background: var(--accent-gold-soft);
+      border: 1px solid rgba(var(--accent-gold-rgb), 0.25);
+      color: var(--accent-gold-dark);
+      border-radius: 999px;
+      font-size: 0.82rem;
       font-weight: 700;
       white-space: nowrap;
-      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
+      box-shadow: var(--shadow-sm);
+      align-self: flex-start;
     }
 
     .scope-features-list {
       list-style: none;
       display: flex;
       flex-direction: column;
-      gap: 0.95rem;
-      margin-top: 1.75rem;
+      gap: 0.85rem;
+      margin-top: 1rem;
     }
 
     .scope-features-list li {
       display: flex;
       align-items: flex-start;
-      gap: 0.85rem;
+      gap: 0.75rem;
     }
 
     .feature-icon {
-      color: var(--accent-gold);
-      margin-top: 0.25rem;
+      color: var(--accent-blue);
+      margin-top: 0.2rem;
       flex-shrink: 0;
     }
 
-    .feature-text {
-      color: var(--text-secondary);
-      font-size: 1rem;
-      line-height: 1.6;
-    }
-
+    .feature-text,
     .scope-description {
-      color: var(--text-secondary);
-      font-size: 1rem;
-      line-height: 1.7;
+      color: var(--ink-secondary);
+      font-size: 0.98rem;
+      line-height: 1.65;
     }
 
-    /* FINANCIAL CONDITIONS */
     .financials-section {
-      background-color: var(--bg-card);
-      border: 1px solid rgba(var(--accent-gold-rgb), 0.15);
-      border-radius: var(--radius-lg);
-      padding: 3rem;
-      position: relative;
-      overflow: hidden;
-      box-shadow: var(--shadow-card);
-    }
-
-    .financials-section::before {
-      content: "";
-      position: absolute;
-      width: 400px;
-      height: 400px;
-      background: radial-gradient(circle, rgba(var(--accent-gold-rgb), 0.035) 0%, rgba(0, 0, 0, 0) 70%);
-      top: -200px;
-      right: -200px;
-      pointer-events: none;
+      padding: 2rem;
+      border-left: 4px solid var(--accent-gold);
+      break-inside: avoid;
+      page-break-inside: avoid;
     }
 
     .financial-grid {
       display: grid;
-      grid-template-columns: 1fr;
-      gap: 2.5rem;
+      gap: 2rem;
     }
 
     .total-invest-block {
       display: flex;
       flex-direction: column;
-      gap: 0.5rem;
-      border-bottom: 1px solid var(--border-color);
-      padding-bottom: 2rem;
+      gap: 0.45rem;
+      padding-bottom: 1.4rem;
+      border-bottom: 1px solid rgba(10, 22, 40, 0.08);
     }
 
     .total-invest-label {
-      font-size: 0.8rem;
+      font-size: 0.76rem;
       text-transform: uppercase;
-      letter-spacing: 0.2em;
-      color: var(--text-muted);
+      letter-spacing: 0.14em;
+      color: var(--ink-muted);
       font-weight: 700;
     }
 
     .total-invest-value {
-      font-size: clamp(2.75rem, 6vw, 4rem);
+      font-size: clamp(2.6rem, 6vw, 4rem);
       font-weight: 800;
-      color: var(--text-primary);
       line-height: 1;
-      letter-spacing: -0.03em;
+      letter-spacing: -0.04em;
+      color: var(--ink-primary);
     }
 
     .total-invest-value span {
-      color: var(--accent-gold);
+      color: var(--accent-gold-dark);
       font-family: 'Instrument Serif', serif;
-      font-weight: 400;
       font-style: italic;
+      font-weight: 400;
     }
 
     .payment-conditions-block {
       display: flex;
       flex-direction: column;
-      gap: 1rem;
+      gap: 0.9rem;
     }
 
     .payment-label {
-      font-size: 0.85rem;
+      font-size: 0.78rem;
       font-weight: 700;
       text-transform: uppercase;
-      letter-spacing: 0.15em;
-      color: var(--accent-gold);
+      letter-spacing: 0.14em;
+      color: var(--accent-blue);
     }
 
     .payment-conditions-list {
       list-style: none;
       display: flex;
       flex-direction: column;
-      gap: 0.95rem;
-      margin-top: 0.5rem;
+      gap: 0.8rem;
+    }
+
+    .payment-conditions-list li {
+      display: flex;
+      align-items: flex-start;
+      gap: 0.75rem;
     }
 
     .payment-bullet {
-      width: 6px;
-      height: 6px;
-      background-color: var(--accent-gold);
+      width: 7px;
+      height: 7px;
+      background: var(--accent-gold);
       border-radius: 50%;
       flex-shrink: 0;
-      box-shadow: 0 0 8px var(--accent-gold);
-    }
-
-    .payment-item-text {
-      color: var(--text-secondary);
-      font-size: 1.05rem;
-      line-height: 1.5;
+      margin-top: 0.55rem;
     }
 
+    .payment-item-text,
     .payment-text {
-      color: var(--text-secondary);
-      font-size: 1.05rem;
+      color: var(--ink-secondary);
+      font-size: 1rem;
       line-height: 1.6;
     }
 
-    /* SIGNATURE BLOCK */
     .signature-section {
-      background-color: var(--bg-card);
-      border: 1px solid var(--border-color);
-      border-radius: var(--radius-lg);
-      padding: 3rem;
-      margin-top: 3rem;
+      margin-top: 2rem;
+      padding: 2rem;
       display: flex;
       flex-direction: column;
       align-items: center;
-      gap: 2rem;
-      box-shadow: var(--shadow-card);
-      position: relative;
+      gap: 1.5rem;
+      break-inside: avoid;
+      page-break-inside: avoid;
     }
 
     .signature-title {
-      font-size: 1.5rem;
+      font-size: 1.4rem;
       font-weight: 700;
-      color: var(--text-primary);
+      color: var(--ink-primary);
       text-align: center;
-      letter-spacing: -0.01em;
+      letter-spacing: -0.02em;
     }
 
     .signature-subtitle {
+      max-width: 520px;
       font-size: 0.95rem;
-      color: var(--text-secondary);
+      color: var(--ink-secondary);
       text-align: center;
-      max-width: 460px;
-      line-height: 1.6;
+      line-height: 1.7;
     }
 
     .signature-pad-container {
       width: 100%;
-      max-width: 500px;
-      background-color: #ffffff !important;
-      border-radius: var(--radius-md);
+      max-width: 520px;
+      border-radius: var(--radius);
       overflow: hidden;
-      border: 1px solid rgba(var(--accent-gold-rgb), 0.25);
-      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
-      position: relative;
+      border: 1px solid rgba(var(--accent-blue-rgb), 0.18);
+      background: linear-gradient(180deg, #ffffff 0%, #fbfcfe 100%);
+      box-shadow: var(--shadow-md);
       transition: var(--transition);
     }
-    
+
     .signature-pad-container:hover {
-      border-color: rgba(var(--accent-gold-rgb), 0.5);
-      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
+      border-color: rgba(var(--accent-blue-rgb), 0.28);
+      box-shadow: var(--shadow-lg);
     }
 
     .signature-pad-container canvas {
@@ -764,93 +829,110 @@
 
     .signature-actions {
       display: flex;
-      gap: 1.25rem;
+      gap: 1rem;
       width: 100%;
-      max-width: 500px;
+      max-width: 520px;
       justify-content: space-between;
       align-items: center;
     }
-    
+
     .clear-btn {
-      background: transparent;
+      background: #ffffff;
       border: 1px solid var(--border-color);
-      color: var(--text-secondary);
-      padding: 0.6rem 1.25rem;
-      border-radius: 99px;
-      font-size: 0.85rem;
-      font-weight: 600;
+      color: var(--ink-secondary);
+      padding: 0.7rem 1.15rem;
+      border-radius: 999px;
+      font-size: 0.83rem;
+      font-weight: 700;
       cursor: pointer;
       transition: var(--transition);
+      box-shadow: var(--shadow-sm);
     }
-    
+
     .clear-btn:hover {
-      border-color: var(--text-primary);
-      color: var(--text-primary);
-      background-color: rgba(255, 255, 255, 0.03);
+      border-color: var(--accent-blue);
+      color: var(--accent-blue);
+      background: var(--accent-blue-soft);
     }
 
     .print-signature-box {
       display: none;
+      flex-direction: column;
+      align-items: center;
+      gap: 0.85rem;
+      width: 100%;
+      max-width: 520px;
+      padding: 1.4rem;
+      border-radius: var(--radius);
+      background: rgba(255, 255, 255, 0.88);
+      border: 1px solid rgba(var(--accent-blue-rgb), 0.16);
+      box-shadow: var(--shadow-sm);
     }
-    
+
     .print-signature-img {
-      max-width: 300px;
+      max-width: 320px;
       max-height: 120px;
       display: block;
       margin: 0 auto;
+      object-fit: contain;
+    }
+
+    .signature-note {
+      color: var(--ink-muted);
+      font-size: 0.75rem;
+      margin-top: 0.15rem;
+      text-align: center;
     }
 
     .print-signatures {
-      display: none; /* Only visible in print */
+      display: none;
     }
 
-    /* FOOTER ACTIONS (CTA & PDF) */
     .proposal-actions {
       display: flex;
       flex-direction: column;
       align-items: center;
-      gap: 1.5rem;
-      border-top: 1px solid var(--border-color);
-      padding-top: 4rem;
-      margin-top: 2rem;
+      gap: 1.1rem;
+      margin-top: 3rem;
+      padding-top: 2rem;
+      border-top: 1px solid rgba(10, 22, 40, 0.07);
     }
 
     .accept-btn {
       display: inline-flex;
       align-items: center;
       justify-content: center;
-      gap: 0.75rem;
+      gap: 0.7rem;
       width: 100%;
-      max-width: 440px;
-      padding: 1.35rem;
-      background: linear-gradient(135deg, var(--text-primary) 0%, #e1e3e8 100%);
-      color: var(--bg-dark);
+      max-width: 460px;
+      padding: 1.2rem 1.5rem;
+      background: linear-gradient(135deg, var(--ink-primary) 0%, #17243a 100%);
+      color: #ffffff;
       text-decoration: none;
-      border-radius: 99px;
+      border-radius: 999px;
       font-weight: 700;
-      font-size: 1.1rem;
-      letter-spacing: 0.01em;
+      font-size: 1rem;
+      letter-spacing: 0.02em;
       cursor: pointer;
       transition: var(--transition);
-      box-shadow: 0 10px 30px rgba(255, 255, 255, 0.08);
-      border: 1px solid transparent;
+      box-shadow: 0 10px 25px rgba(10, 22, 40, 0.16);
+      border: none;
     }
 
     .accept-btn.disabled {
-      opacity: 0.35;
+      opacity: 0.4;
       pointer-events: none;
-      filter: grayscale(100%);
+      filter: grayscale(0.5);
     }
 
     .accept-btn:hover {
-      background: linear-gradient(135deg, var(--accent-gold) 0%, #b88d5e 100%);
-      color: var(--bg-dark);
-      transform: translateY(-3px);
-      box-shadow: 0 15px 35px rgba(212, 168, 122, 0.25);
+      background: linear-gradient(135deg, var(--accent-blue) 0%, #1e4b85 100%);
+      transform: translateY(-2px);
+      box-shadow: 0 16px 34px rgba(var(--accent-blue-rgb), 0.24);
     }
 
     .accept-btn svg {
-      transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1);
+      transition: transform 0.25s ease;
     }
 
     .accept-btn:hover svg {
@@ -858,484 +940,644 @@
     }
 
     .print-trigger-btn {
-      background: transparent;
+      background: #ffffff;
       border: 1px solid var(--border-color);
-      color: var(--text-secondary);
-      padding: 0.85rem 1.75rem;
-      border-radius: 99px;
-      font-size: 0.9rem;
-      font-weight: 600;
+      color: var(--accent-blue);
+      padding: 0.85rem 1.45rem;
+      border-radius: 999px;
+      font-size: 0.88rem;
+      font-weight: 700;
       cursor: pointer;
-      display: flex;
+      display: inline-flex;
       align-items: center;
-      gap: 0.6rem;
+      gap: 0.55rem;
       transition: var(--transition);
+      box-shadow: var(--shadow-sm);
     }
 
     .print-trigger-btn:hover {
-      border-color: var(--text-primary);
-      color: var(--text-primary);
-      background-color: rgba(255, 255, 255, 0.03);
+      border-color: var(--accent-blue);
+      background: var(--accent-blue-soft);
+      transform: translateY(-1px);
+      box-shadow: var(--shadow-md);
     }
 
     .disclaimer {
       font-size: 0.8rem;
-      color: var(--text-muted);
+      color: var(--ink-muted);
       text-align: center;
-      max-width: 520px;
+      max-width: 560px;
       line-height: 1.6;
     }
 
-    /* ═══════════════════════════════════════════════════
-       MEDIA PRINT (IMPRESSÃO PDF PERFEITA A4)
-       ═══════════════════════════════════════════════════ */
-    @media print {
-      @page {
-        size: A4;
-        margin: 20mm 20mm 20mm 20mm;
-      }
+    .proposal-view {
+      background: transparent;
+      border: none;
+      box-shadow: none;
+      padding: 0;
+      overflow: visible;
+    }
 
-      html, body {
-        background-color: #ffffff !important;
-        color: #000000 !important;
-        padding: 0 !important;
-        margin: 0 !important;
-        font-family: 'Inter', sans-serif !important;
-        font-size: 11pt !important;
-        line-height: 1.5 !important;
-        overflow: visible !important;
-      }
+    .proposal-view::before {
+      display: none;
+    }
 
-      .page-wrapper {
-        padding: 0 !important;
-        min-height: auto !important;
-        overflow: visible !important;
-        display: block !important;
-      }
+    .proposal-pages {
+      display: flex;
+      flex-direction: column;
+      gap: 1.75rem;
+      align-items: center;
+    }
 
-      .ambient-light, 
-      .bg-grid,
-      .bg-glow-1,
-      .bg-glow-2,
-      .proposal-actions, 
-      .print-trigger-btn, 
-      .accept-btn,
-      .scope-price-badge:empty,
-      #errorScreen,
-      .premium-checkbox-container {
-        display: none !important;
-      }
+    .proposal-sheet {
+      width: min(100%, 820px);
+      min-height: 1120px;
+      background: #ffffff;
+      border: 1px solid rgba(var(--accent-blue-rgb), 0.14);
+      box-shadow: 0 20px 50px rgba(10, 22, 40, 0.08);
+      padding: 3.2rem 3rem;
+      position: relative;
+      break-after: page;
+      page-break-after: always;
+      display: flex;
+      flex-direction: column;
+      overflow: hidden;
+    }
 
-      .scope-select-wrap {
-        gap: 0 !important;
-      }
+    .proposal-sheet:last-child {
+      page-break-after: auto;
+      break-after: auto;
+    }
 
-      .container {
-        max-width: 100% !important;
-        width: 100% !important;
-        margin: 0 !important;
-        padding: 0 !important;
-      }
+    .proposal-sheet::before {
+      content: '';
+      position: absolute;
+      inset: 0;
+      border: 1px solid rgba(var(--accent-blue-rgb), 0.08);
+      pointer-events: none;
+      margin: 18px;
+    }
 
-      /* Cabeçalho de Branding Centralizado Estilo Editorial */
-      .brand-header {
-        border-bottom: 2px solid #111111 !important;
-        margin-bottom: 3.5rem !important;
-        padding-bottom: 1.25rem !important;
-        display: flex !important;
-        justify-content: space-between !important;
-        align-items: flex-end !important;
-      }
+    .sheet-brand-row {
+      display: flex;
+      justify-content: space-between;
+      align-items: center;
+      gap: 1rem;
+      margin-bottom: 2.75rem;
+    }
 
-      .brand-logo {
-        color: #000000 !important;
-        font-size: 20pt !important;
-        font-weight: 800 !important;
-      }
+    .sheet-brand {
+      font-family: 'Instrument Serif', Georgia, serif;
+      font-style: italic;
+      font-size: 3rem;
+      color: var(--ink-primary);
+      line-height: 1;
+      display: flex;
+      align-items: baseline;
+      gap: 0.55rem;
+    }
 
-      .brand-tag {
-        color: #444444 !important;
-        font-size: 9pt !important;
-        font-weight: 600 !important;
-        text-transform: uppercase !important;
-        letter-spacing: 0.15em !important;
-      }
+    .sheet-brand::after {
+      content: '';
+      width: 8px;
+      height: 8px;
+      border-radius: 50%;
+      background: var(--accent-blue);
+      display: inline-block;
+      margin-bottom: 0.35rem;
+    }
 
-      /* Hero Cover */
-      .proposal-hero {
-        margin-bottom: 3.5rem !important;
-        text-align: left !important;
-        page-break-inside: avoid !important;
-      }
+    .sheet-brand-tag {
+      font-size: 0.68rem;
+      letter-spacing: 0.18em;
+      text-transform: uppercase;
+      color: rgba(10, 22, 40, 0.45);
+      font-weight: 700;
+      text-align: right;
+    }
 
-      .proposal-meta {
-        color: #8c6239 !important;
-        font-size: 11pt !important;
-        font-weight: 700 !important;
-        text-transform: uppercase !important;
-        letter-spacing: 0.15em !important;
-        margin-bottom: 0.5rem !important;
-      }
+    .cover-sheet {
+      justify-content: space-between;
+      padding-top: 3.6rem;
+    }
 
-      .proposal-title {
-        color: #000000 !important;
-        font-family: 'Instrument Serif', serif !important;
-        font-size: 32pt !important;
-        font-style: italic !important;
-        font-weight: 400 !important;
-        line-height: 1.1 !important;
-        margin-bottom: 2rem !important;
-      }
+    .cover-heading {
+      text-align: center;
+      margin-top: 2rem;
+      margin-bottom: 1.5rem;
+    }
 
-      .proposal-client-card {
-        border-left: 3px solid #8c6239 !important;
-        padding-left: 1.5rem !important;
-        margin-top: 0.5rem !important;
-        display: flex !important;
-        flex-direction: column !important;
-        gap: 0.2rem !important;
-      }
+    .cover-kicker {
+      font-size: 0.95rem;
+      font-weight: 800;
+      text-transform: uppercase;
+      letter-spacing: 0.04em;
+      color: #1d2238;
+      margin-bottom: 0.9rem;
+    }
 
-      .client-label {
-        color: #555555 !important;
-        font-size: 8.5pt !important;
-        font-weight: 600 !important;
-        text-transform: uppercase !important;
-        letter-spacing: 0.1em !important;
-      }
+    .cover-subtitle {
+      font-size: 1.15rem;
+      color: rgba(10, 22, 40, 0.66);
+      margin-bottom: 1.6rem;
+    }
 
-      .client-company {
-        color: #000000 !important;
-        font-size: 16pt !important;
-        font-weight: 700 !important;
-      }
+    .cover-divider,
+    .sheet-foot-divider {
+      width: 100%;
+      height: 2px;
+      background: linear-gradient(90deg, rgba(var(--accent-blue-rgb), 0.08) 0%, rgba(var(--accent-blue-rgb), 0.55) 18%, rgba(var(--accent-blue-rgb), 0.55) 82%, rgba(var(--accent-blue-rgb), 0.08) 100%);
+    }
 
-      .client-name {
-        color: #444444 !important;
-        font-size: 10.5pt !important;
-      }
+    .cover-grid {
+      display: grid;
+      grid-template-columns: repeat(2, minmax(0, 1fr));
+      gap: 2rem;
+      margin: 2.5rem 0 3rem;
+    }
 
-      /* Métricas em Print */
-      .metrics-grid {
-        grid-template-columns: repeat(3, 1fr) !important;
-        gap: 1.5rem !important;
-        margin-bottom: 3.5rem !important;
-        page-break-inside: avoid !important;
-      }
+    .cover-card {
+      border: 1.5px solid rgba(var(--accent-blue-rgb), 0.24);
+      padding: 2.35rem 2rem;
+      min-height: 220px;
+      display: flex;
+      flex-direction: column;
+      justify-content: center;
+      text-align: center;
+      background: linear-gradient(180deg, rgba(255,255,255,0.99) 0%, rgba(36,91,161,0.025) 100%);
+    }
 
-      .metric-card {
-        background-color: #f9f9fb !important;
-        border: 1px solid #e1e1e6 !important;
-        border-radius: 12px !important;
-        padding: 1.25rem !important;
-        box-shadow: none !important;
-        transform: none !important;
-      }
+    .cover-card-title {
+      font-size: 1rem;
+      font-weight: 800;
+      color: #1d2238;
+      margin-bottom: 0.8rem;
+    }
 
-      .metric-label {
-        color: #555555 !important;
-        font-size: 8pt !important;
-      }
+    .cover-card-meta,
+    .cover-card-detail {
+      font-size: 0.92rem;
+      color: rgba(10, 22, 40, 0.68);
+      margin-bottom: 0.65rem;
+    }
 
-      .metric-value {
-        color: #000000 !important;
-        font-size: 13pt !important;
-      }
+    .cover-card-page {
+      margin-top: 1rem;
+      padding-top: 0.75rem;
+      border-top: 1px solid rgba(var(--accent-blue-rgb), 0.18);
+      font-size: 0.95rem;
+      color: var(--accent-blue);
+      font-weight: 700;
+    }
 
-      .metric-value.highlight-gold {
-        color: #8c6239 !important;
-        font-size: 18pt !important;
-      }
+    .cover-footer-note {
+      display: flex;
+      flex-direction: column;
+      gap: 0.6rem;
+      align-items: center;
+      text-align: center;
+      color: rgba(10, 22, 40, 0.68);
+      font-size: 0.98rem;
+      line-height: 1.65;
+    }
 
-      .section-title {
-        color: #8c6239 !important;
-        font-family: 'Instrument Serif', serif !important;
-        font-size: 18pt !important;
-        font-style: italic !important;
-        font-weight: 400 !important;
-        margin-top: 3.5rem !important;
-        margin-bottom: 1.5rem !important;
-        page-break-after: avoid !important;
-        display: flex !important;
-        align-items: center !important;
-        gap: 0.5rem !important;
-      }
+    .sheet-section-bar {
+      background: linear-gradient(135deg, var(--ink-primary) 0%, #17355f 100%);
+      color: #ffffff;
+      font-size: 0.92rem;
+      font-weight: 800;
+      letter-spacing: 0.04em;
+      text-transform: uppercase;
+      text-align: center;
+      padding: 0.95rem 1rem;
+      margin-bottom: 1rem;
+    }
 
-      .section-title::after {
-        content: "" !important;
-        flex: 1 !important;
-        height: 1px !important;
-        background-color: #dddddd !important;
-      }
+    .sheet-stack {
+      display: flex;
+      flex-direction: column;
+      gap: 0.85rem;
+    }
 
-      .intro-card {
-        background-color: transparent !important;
-        border: none !important;
-        padding: 0 !important;
-        margin-bottom: 2.5rem !important;
-        page-break-inside: avoid !important;
-      }
+    .package-card {
+      border: 1.5px solid rgba(var(--accent-blue-rgb), 0.18);
+      background: linear-gradient(180deg, rgba(255,255,255,0.99) 0%, rgba(36,91,161,0.022) 100%);
+      padding: 1.25rem 1.3rem 1.1rem;
+      display: flex;
+      flex-direction: column;
+      gap: 0.85rem;
+    }
 
-      .intro-text {
-        color: #222222 !important;
-        font-size: 11pt !important;
-        line-height: 1.6 !important;
-      }
+    .package-header {
+      display: flex;
+      justify-content: space-between;
+      align-items: flex-start;
+      gap: 1rem;
+      padding-bottom: 0.75rem;
+      border-bottom: 1px solid rgba(var(--accent-blue-rgb), 0.14);
+    }
 
-      /* Objetivos no Formato de Lista Limpa */
-      .objectives-grid {
-        display: flex !important;
-        flex-direction: column !important;
-        gap: 0.85rem !important;
-        margin-bottom: 2.5rem !important;
-        page-break-inside: avoid !important;
-      }
+    .package-heading {
+      display: flex;
+      align-items: flex-start;
+      gap: 0.75rem;
+      min-width: 0;
+      flex: 1;
+    }
 
-      .objective-card {
-        background-color: transparent !important;
-        border: none !important;
-        padding: 0 !important;
-        margin: 0 !important;
-        display: flex !important;
-        gap: 0.75rem !important;
-        align-items: flex-start !important;
-        box-shadow: none !important;
-        transform: none !important;
-      }
+    .package-checkbox {
+      width: 20px;
+      height: 20px;
+      appearance: none;
+      border: 1.5px solid rgba(31, 32, 55, 0.3);
+      border-radius: 4px;
+      margin-top: 0.15rem;
+      background: #ffffff;
+      position: relative;
+      flex-shrink: 0;
+      cursor: pointer;
+    }
 
-      .objective-num-wrapper {
-        background-color: transparent !important;
-        border: none !important;
-        width: auto !important;
-        height: auto !important;
-        border-radius: 0 !important;
-        padding: 0 !important;
-      }
+    .package-checkbox:checked {
+      background: #1f2037;
+      border-color: #1f2037;
+    }
 
-      .objective-num {
-        font-family: 'Inter', sans-serif !important;
-        font-size: 11pt !important;
-        font-style: normal !important;
-        font-weight: 700 !important;
-        color: #8c6239 !important;
-      }
+    .package-checkbox:checked::after {
+      content: '';
+      position: absolute;
+      left: 6px;
+      top: 2px;
+      width: 5px;
+      height: 10px;
+      border: solid #ffffff;
+      border-width: 0 2px 2px 0;
+      transform: rotate(45deg);
+    }
 
-      .objective-num::after {
-        content: "." !important;
-      }
+    .package-title-wrap {
+      min-width: 0;
+    }
 
-      .objective-content {
-        font-family: 'Inter', sans-serif !important;
-        font-size: 11pt !important;
-        color: #222222 !important;
-        line-height: 1.5 !important;
-      }
+    .package-title {
+      font-size: 1rem;
+      font-weight: 800;
+      color: #1f2037;
+      line-height: 1.3;
+    }
 
-      /* Serviços / Escopo no Formato Escrito */
-      .scope-list {
-        gap: 2rem !important;
-      }
+    .package-subline {
+      font-size: 0.82rem;
+      color: rgba(10, 22, 40, 0.58);
+      font-style: italic;
+      margin-top: 0.2rem;
+    }
 
-      .scope-card {
-        background-color: transparent !important;
-        border: none !important;
-        border-bottom: 1px dashed #dddddd !important;
-        border-radius: 0 !important;
-        padding: 0 0 2rem 0 !important;
-        margin-bottom: 2rem !important;
-        box-shadow: none !important;
-        page-break-inside: avoid !important;
-      }
+    .package-price {
+      font-size: 1rem;
+      font-weight: 800;
+      color: var(--accent-blue);
+      white-space: nowrap;
+      text-align: right;
+      line-height: 1.2;
+    }
 
-      .scope-header {
-        margin-bottom: 1rem !important;
-        display: flex !important;
-        flex-direction: column !important;
-        align-items: flex-start !important;
-        gap: 0.25rem !important;
-      }
+    .package-list {
+      list-style: none;
+      display: flex;
+      flex-direction: column;
+      gap: 0.34rem;
+      color: rgba(10, 22, 40, 0.78);
+      font-size: 0.87rem;
+      line-height: 1.45;
+    }
 
-      .scope-num {
-        color: #8c6239 !important;
-        font-size: 8.5pt !important;
-        font-weight: 700 !important;
-        letter-spacing: 0.1em !important;
-      }
+    .package-list li {
+      padding-left: 0.8rem;
+      position: relative;
+    }
 
-      .scope-title {
-        color: #000000 !important;
-        font-size: 13pt !important;
-        font-weight: 700 !important;
-        letter-spacing: -0.01em !important;
-      }
+    .package-list li::before {
+      content: '•';
+      position: absolute;
+      left: 0;
+      color: #1f2037;
+    }
 
-      .scope-price-badge {
-        background-color: transparent !important;
-        border: none !important;
-        padding: 0 !important;
-        color: #8c6239 !important;
-        font-size: 11pt !important;
-        font-weight: 700 !important;
-        margin-top: 0.25rem !important;
-      }
+    .notes-layout {
+      display: grid;
+      grid-template-columns: 1fr;
+      gap: 1.2rem;
+      margin-top: 1rem;
+    }
 
-      .scope-price-badge::before {
-        content: "Investimento: " !important;
-        color: #555555 !important;
-        font-weight: 500 !important;
-      }
+    .notes-box {
+      border: 1.5px solid rgba(var(--accent-blue-rgb), 0.18);
+      background: rgba(36, 91, 161, 0.03);
+      padding: 1.35rem 1.35rem 1.2rem;
+      display: flex;
+      flex-direction: column;
+      gap: 0.8rem;
+    }
 
-      .scope-features-list {
-        margin-top: 1rem !important;
-        gap: 0.5rem !important;
-      }
+    .notes-title {
+      font-size: 0.96rem;
+      color: var(--accent-blue);
+      font-weight: 800;
+    }
 
-      .scope-features-list li {
-        gap: 0.5rem !important;
-      }
+    .notes-text {
+      color: rgba(10, 22, 40, 0.74);
+      font-size: 0.9rem;
+      line-height: 1.6;
+    }
 
-      .feature-icon {
-        display: none !important;
-      }
+    .notes-columns {
+      display: grid;
+      grid-template-columns: repeat(2, minmax(0, 1fr));
+      gap: 1rem;
+    }
 
-      .scope-features-list li::before {
-        content: "•" !important;
-        color: #8c6239 !important;
-        font-size: 12pt !important;
-        margin-right: 0.25rem !important;
-        line-height: 1 !important;
-        margin-top: 0.1rem !important;
-      }
+    .notes-list {
+      list-style: none;
+      display: flex;
+      flex-direction: column;
+      gap: 0.45rem;
+      color: rgba(10, 22, 40, 0.76);
+      font-size: 0.88rem;
+      line-height: 1.5;
+    }
 
-      .feature-text {
-        color: #222222 !important;
-        font-size: 10.5pt !important;
-        line-height: 1.4 !important;
-      }
+    .notes-list li {
+      padding-left: 0.85rem;
+      position: relative;
+    }
 
-      /* Condições de Pagamento e Valores */
-      .financials-section {
-        background-color: transparent !important;
-        border: none !important;
-        padding: 0 !important;
-        margin-bottom: 3rem !important;
-        page-break-inside: avoid !important;
-      }
+    .notes-list li::before {
+      content: '•';
+      position: absolute;
+      left: 0;
+      color: var(--accent-blue);
+    }
 
-      .total-invest-block {
-        border-bottom: 1px solid #111111 !important;
-        padding-bottom: 1rem !important;
-        margin-bottom: 1.5rem !important;
-      }
+    .sheet-contact-line {
+      margin-top: auto;
+      padding-top: 1rem;
+      text-align: center;
+      font-size: 0.95rem;
+      color: rgba(10, 22, 40, 0.66);
+      font-style: italic;
+    }
 
-      .total-invest-label {
-        color: #555555 !important;
-        font-size: 9pt !important;
-      }
+    .commercial-grid {
+      display: grid;
+      grid-template-columns: 1fr;
+      gap: 0.9rem;
+      margin-top: 1rem;
+    }
 
-      .total-invest-value {
-        color: #000000 !important;
-        font-size: 26pt !important;
-      }
+    .signature-sheet {
+      justify-content: space-between;
+    }
 
-      .total-invest-value span {
-        color: #8c6239 !important;
-      }
+    .signature-sheet-main {
+      display: flex;
+      flex-direction: column;
+      gap: 1.2rem;
+      margin-top: 1.3rem;
+    }
 
-      .payment-label {
-        color: #8c6239 !important;
-        font-size: 11pt !important;
-        font-weight: 700 !important;
-      }
+    .signature-copy {
+      text-align: center;
+      color: rgba(10, 22, 40, 0.7);
+      font-size: 0.98rem;
+      line-height: 1.65;
+      max-width: 540px;
+      align-self: center;
+    }
+
+    .signature-meta-grid {
+      display: grid;
+      grid-template-columns: repeat(2, minmax(0, 1fr));
+      gap: 1rem;
+    }
+
+    .signature-meta-card {
+      border: 1.5px solid rgba(var(--accent-blue-rgb), 0.18);
+      background: rgba(36, 91, 161, 0.028);
+      padding: 1rem 1.1rem;
+      min-height: 128px;
+    }
+
+    .signature-meta-label {
+      font-size: 0.76rem;
+      text-transform: uppercase;
+      letter-spacing: 0.08em;
+      color: rgba(10, 22, 40, 0.5);
+      font-weight: 700;
+      margin-bottom: 0.6rem;
+    }
+
+    .signature-meta-value {
+      color: #1f2037;
+      font-size: 1rem;
+      line-height: 1.5;
+      font-weight: 700;
+    }
+
+    .signature-section {
+      background: transparent;
+      border: 1.5px solid rgba(var(--accent-blue-rgb), 0.18);
+      border-radius: 0;
+      box-shadow: none;
+      padding: 1.5rem;
+      margin-top: 0.4rem;
+      align-items: stretch;
+      gap: 1rem;
+    }
+
+    .signature-section::before {
+      display: none;
+    }
+
+    .signature-title {
+      text-align: left;
+      color: #1f2037;
+      font-size: 1.1rem;
+      font-weight: 800;
+    }
+
+    .signature-subtitle {
+      max-width: none;
+      text-align: left;
+      font-size: 0.9rem;
+    }
+
+    .signature-pad-container,
+    .print-signature-box {
+      max-width: none;
+      width: 100%;
+    }
+
+    .signature-pad-container {
+      border-radius: 0;
+      border: 1px solid rgba(var(--accent-blue-rgb), 0.16);
+      box-shadow: none;
+    }
+
+    .print-signature-box {
+      border-radius: 0;
+      border: 1px solid rgba(var(--accent-blue-rgb), 0.16);
+      box-shadow: none;
+      align-items: flex-start;
+      background: rgba(255, 255, 255, 0.96);
+    }
+
+    .signature-actions {
+      max-width: none;
+    }
+
+    .signature-footer-note {
+      text-align: center;
+      color: rgba(10, 22, 40, 0.62);
+      font-size: 0.9rem;
+      line-height: 1.6;
+      font-style: italic;
+    }
+
+    body.pdf-exporting .proposal-actions,
+    body.pdf-exporting .signature-actions,
+    body.pdf-exporting .signature-pad-container,
+    body.pdf-exporting .premium-checkbox-container {
+      display: none !important;
+    }
+
+    body.pdf-exporting .proposal-sheet {
+      box-shadow: none;
+      width: 210mm;
+      min-height: 297mm;
+      margin: 0;
+    }
 
-      .payment-conditions-list {
-        gap: 0.5rem !important;
-        margin-top: 0.5rem !important;
+    body.pdf-exporting .proposal-view {
+      box-shadow: none;
+      border-radius: 0;
+    }
+
+    body.pdf-exporting .proposal-pages {
+      gap: 0;
+    }
+
+    body.pdf-exporting .proposal-sheet::before {
+      margin: 12px;
+    }
+
+    body.pdf-exporting .package-card,
+    body.pdf-exporting .cover-card,
+    body.pdf-exporting .notes-box,
+    body.pdf-exporting .signature-meta-card {
+      transform: none !important;
+      box-shadow: none !important;
+    }
+
+    @media print {
+      @page {
+        size: A4;
+        margin: 12mm;
       }
 
-      .payment-bullet {
-        background-color: #8c6239 !important;
-        width: 5px !important;
-        height: 5px !important;
+      html,
+      body {
+        background: #ffffff !important;
+        color: var(--ink-primary) !important;
+        margin: 0 !important;
+        padding: 0 !important;
+        overflow: visible !important;
+        -webkit-print-color-adjust: exact;
+        print-color-adjust: exact;
       }
 
-      .payment-item-text {
-        color: #222222 !important;
-        font-size: 11pt !important;
+      .page-wrapper {
+        padding: 0 !important;
+        min-height: auto !important;
       }
 
-      /* Bloco de Assinaturas Perfeito */
-      .print-signatures {
-        display: block !important;
-        margin-top: 5rem !important;
-        page-break-inside: avoid !important;
+      .bg-grid,
+      .bg-glow-1,
+      .bg-glow-2,
+      .proposal-actions,
+      #errorScreen {
+        display: none !important;
       }
 
-      .sig-block-title {
-        font-size: 12pt !important;
-        font-weight: 700 !important;
-        letter-spacing: 0.1em !important;
-        margin-bottom: 2rem !important;
-        color: #000000 !important;
-        border-bottom: 1px solid #111111 !important;
-        padding-bottom: 0.4rem !important;
-        text-transform: uppercase !important;
+      .container {
+        width: 100% !important;
+        max-width: 100% !important;
       }
 
-      .sig-text-line {
-        font-size: 11pt !important;
-        color: #000000 !important;
-        margin-bottom: 2.5rem !important;
-        line-height: 1.5 !important;
+      .proposal-view {
+        border-radius: 0 !important;
+        box-shadow: none !important;
+        border: 1px solid var(--border-color) !important;
       }
+    }
 
-      .sig-date-line {
-        font-size: 11pt !important;
-        color: #000000 !important;
-        margin-top: 3rem !important;
-        font-weight: 500 !important;
+    @media (max-width: 920px) {
+      .metrics-grid {
+        grid-template-columns: 1fr;
       }
     }
 
-    /* RESPONSIVE DIGITAL LAYOUT */
     @media (max-width: 768px) {
       .page-wrapper {
-        padding: 3rem 1rem 5rem 1rem;
+        padding: 1.5rem 1rem 4rem;
+      }
+
+      .proposal-view {
+        padding: 1.4rem;
+        border-radius: 24px;
       }
+
       .brand-header {
-        margin-bottom: 3rem;
         flex-direction: column;
         align-items: flex-start;
         gap: 0.75rem;
       }
-      .metrics-grid {
-        grid-template-columns: 1fr;
-        gap: 1rem;
+
+      .proposal-title {
+        margin-bottom: 1.5rem;
       }
-      .metric-card {
-        padding: 1.5rem;
+
+      .proposal-client-card {
+        width: 100%;
+      }
+
+      .intro-card,
+      .financials-section,
+      .signature-section {
+        padding: 1.4rem;
       }
+
       .scope-card {
-        padding: 1.75rem;
+        padding: 1.35rem;
       }
+
       .scope-header {
         flex-direction: column;
         align-items: flex-start;
-        gap: 0.75rem;
       }
+
       .scope-price-badge {
         align-self: flex-start;
       }
-      .financials-section {
-        padding: 1.75rem;
-      }
-      .signature-section {
-        padding: 1.75rem;
-      }
+
       .signature-actions {
         flex-direction: column;
-        gap: 1rem;
-        align-items: center;
+        align-items: stretch;
+      }
+
+      .clear-btn {
+        width: 100%;
       }
     }
   </style>
@@ -1362,112 +1604,7 @@
 
       <!-- PÁGINA DA PROPOSTA OFICIAL -->
       <main class="proposal-view" id="proposalView">
-        
-        <!-- CABEÇALHO BRANDING -->
-        <header class="brand-header">
-          <div class="brand-logo">DNA</div>
-          <div class="brand-tag">Estúdio de Design &amp; Estratégia</div>
-        </header>
-
-        <!-- HERO CAPA -->
-        <section class="proposal-hero">
-          <div class="proposal-meta">Proposta Comercial</div>
-          <h1 class="proposal-title" id="propTitle">Aceleração Digital</h1>
-          
-          <div class="proposal-client-card">
-            <span class="client-label">Preparado especialmente para</span>
-            <span class="client-company" id="clientCompany">Empresa Cliente</span>
-            <span class="client-name" id="clientContact">Aos cuidados de Felipe Silveira</span>
-          </div>
-        </section>
-
-        <!-- MÉTRICAS PRINCIPAIS -->
-        <section class="metrics-grid">
-          <div class="metric-card">
-            <span class="metric-label">Investimento Total</span>
-            <span class="metric-value highlight-gold" id="metricTotal">R$ 0,00</span>
-          </div>
-          <div class="metric-card">
-            <span class="metric-label">Prazo Estimado</span>
-            <span class="metric-value" id="metricDeadline">30 dias</span>
-          </div>
-          <div class="metric-card">
-            <span class="metric-label">Validade Proposta</span>
-            <span class="metric-value" id="metricValidity">7 dias</span>
-          </div>
-        </section>
-
-        <!-- APRESENTAÇÃO INSTITUCIONAL -->
-        <section class="proposal-section" id="introSection">
-          <h2 class="section-title">Apresentação</h2>
-          <div class="intro-card">
-            <p class="intro-text" id="propIntro">A Agência DNA atua na criação de estratégias digitais completas...</p>
-          </div>
-        </section>
-
-        <!-- OBJETIVOS DO PROJETO -->
-        <section class="proposal-section" id="objectivesSection">
-          <h2 class="section-title">Objetivos do Projeto</h2>
-          <div class="objectives-grid" id="objectivesContainer">
-            <!-- Os objetivos dinâmicos serão inseridos aqui -->
-          </div>
-        </section>
-
-        <!-- ESCOPO DETALHADO -->
-        <section class="proposal-section">
-          <h2 class="section-title">Escopo de Trabalho</h2>
-          <div class="scope-list" id="scopeContainer">
-            <!-- Os cartões de escopo dinâmicos serão inseridos aqui -->
-          </div>
-        </section>
-
-        <!-- CONDIÇÕES FINANCEIRAS -->
-        <section class="proposal-section">
-          <h2 class="section-title">Valores &amp; Pagamento</h2>
-          <div class="financials-section">
-            <div class="financial-grid">
-              <div class="total-invest-block">
-                <span class="total-invest-label">Investimento Global</span>
-                <span class="total-invest-value" id="financialTotal">R$ 0,00 <span>*</span></span>
-              </div>
-              <div class="payment-conditions-block">
-                <span class="payment-label">Condições de Pagamento</span>
-                <div class="payment-text" id="financialConditions">
-                  Condições customizadas a serem carregadas dinamicamente...
-                </div>
-              </div>
-            </div>
-          </div>
-        </section>
-
-        <!-- SIGNATURE BLOCK -->
-        <div class="signature-section" id="signatureSection">
-          <h3 class="signature-title">Assinatura Eletrônica</h3>
-          <p class="signature-subtitle">Desenhe sua assinatura no quadro abaixo para validar a aprovação do escopo selecionado e gerar o documento oficial.</p>
-          <div class="signature-pad-container">
-            <canvas id="signaturePad" width="500" height="200"></canvas>
-          </div>
-          <div class="signature-actions">
-            <button type="button" class="clear-btn" id="clearSignatureBtn">Limpar Assinatura</button>
-            <span style="color: #ff4a4a; font-size: 0.85rem; display: none; margin-top: 8px;" id="signatureError">Assinatura obrigatória</span>
-          </div>
-          
-          <!-- Elemento visível apenas no PDF renderizado -->
-          <div class="print-signature-box" id="printSignatureBox">
-            <img src="" id="printSignatureImg" class="print-signature-img" alt="Assinatura">
-            <p style="color: #555; font-size: 0.75rem; margin-top: 0.5rem;" id="printSignatureName">Assinado eletronicamente via DNA Studio</p>
-          </div>
-        </div>
-
-        <!-- SIGNATURE BLOCK (EXCLUSIVO PARA IMPRESSÃO NATIVA) -->
-        <div class="print-signatures" id="printSignatures">
-          <div class="sig-block-title">ASSINATURA</div>
-          <div class="sig-lines-container">
-            <p class="sig-text-line"><strong>CONTRATADA:</strong> DNA Estúdio de Design e Estratégia (_______________________________________)</p>
-            <p class="sig-text-line"><strong>CONTRATANTE:</strong> <span id="sigClientCompany">Empresa Cliente</span> (_______________________________________)</p>
-            <p class="sig-date-line">Praia Grande, ____ de ______________________ de 2026</p>
-          </div>
-        </div>
+        <div class="proposal-pages" id="proposalPages"></div>
 
         <!-- AÇÕES FINAIS (WHATSAPP E PDF) -->
         <footer class="proposal-actions">
@@ -1479,20 +1616,19 @@
             </svg>
           </a>
           
-          <button class="print-trigger-btn" onclick="window.print()">
+          <button class="print-trigger-btn" id="downloadPdfBtn" type="button" onclick="downloadProposalPdf()">
             <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
               <polyline points="6 9 6 2 18 2 18 9"></polyline>
               <path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"></path>
               <rect x="6" y="14" width="12" height="8"></rect>
             </svg>
-            Salvar como PDF / Imprimir
+            Baixar PDF da Proposta
           </button>
 
           <p class="disclaimer">
             Ao clicar em "Aceitar Proposta", você será direcionado ao WhatsApp da nossa equipe de coordenação de projetos para formalizar as assinaturas e agendar a reunião de Kickoff.
           </p>
         </footer>
-
       </main>
     </div>
   </div>
@@ -1508,212 +1644,373 @@
       }
     }
 
-    // Carregar dados da URL
-    window.addEventListener('DOMContentLoaded', () => {
+    function getProposalDataFromUrl() {
       const urlParams = new URLSearchParams(window.location.search);
       const dataParam = urlParams.get('d');
-
       if (!dataParam) {
-        showErrorScreen();
-        return;
+        return null;
       }
 
-      // Decodificar payload
       const decodedJson = b64_to_utf8(dataParam);
       if (!decodedJson) {
-        showErrorScreen();
-        return;
+        return null;
       }
 
       try {
-        const proposal = JSON.parse(decodedJson);
-        renderProposal(proposal);
+        return JSON.parse(decodedJson);
       } catch (err) {
         console.error("Falha ao parsear JSON", err);
+        return null;
+      }
+    }
+
+    window.addEventListener('DOMContentLoaded', () => {
+      const proposal = getProposalDataFromUrl();
+      if (!proposal) {
         showErrorScreen();
+        return;
       }
+
+      renderProposal(proposal);
     });
 
     function showErrorScreen() {
       document.getElementById('errorScreen').style.display = 'flex';
     }
 
-    function renderProposal(data) {
-      // Popular Metadados da Proposta
-      document.getElementById('propTitle').textContent = data.title || 'Proposta Comercial';
-      document.getElementById('clientCompany').textContent = data.company || 'Empresa Cliente';
-      document.getElementById('clientContact').textContent = `Aos cuidados de: ${data.contact || ''}`;
-      
-      // Assinatura Print
-      document.getElementById('sigClientCompany').textContent = data.company || 'Empresa Cliente';
-
-      // Popular Cards Métricas
-      document.getElementById('metricDeadline').textContent = data.deadline || 'A definir';
-      document.getElementById('metricValidity').textContent = data.validity || 'A definir';
-
-      // Popular Apresentação (Apenas se preenchido)
-      const introSection = document.getElementById('introSection');
-      if (data.intro && data.intro.trim() !== '') {
-        document.getElementById('propIntro').textContent = data.intro;
-        introSection.style.display = 'block';
-      } else {
-        introSection.style.display = 'none';
+    function escapeHtml(value) {
+      return String(value ?? '')
+        .replace(/&/g, '&amp;')
+        .replace(/</g, '&lt;')
+        .replace(/>/g, '&gt;')
+        .replace(/"/g, '&quot;')
+        .replace(/'/g, '&#39;');
+    }
+
+    function splitLines(value) {
+      return String(value || '')
+        .split('\n')
+        .map(line => line.trim())
+        .filter(Boolean);
+    }
+
+    function chunkItems(items, size) {
+      const chunks = [];
+      for (let i = 0; i < items.length; i += size) {
+        chunks.push(items.slice(i, i + size));
       }
+      return chunks;
+    }
+
+    function buildCoverPage(data, refs) {
+      const company = escapeHtml(data.company || 'Empresa Cliente');
+      const contact = escapeHtml(data.contact || 'Contato principal');
+      const subtitle = escapeHtml(data.title || `Proposta comercial personalizada para ${data.company || 'o seu negócio'}`);
+
+      return `
+        <section class="proposal-sheet cover-sheet">
+          <div>
+            <div class="sheet-brand-row">
+              <div class="sheet-brand">DNA</div>
+              <div class="sheet-brand-tag">Estúdio de Design &amp; Estratégia</div>
+            </div>
+
+            <div class="cover-heading">
+              <div class="cover-kicker">APRESENTAÇÃO DE SERVIÇOS</div>
+              <div class="cover-subtitle">${subtitle}</div>
+              <div class="cover-divider"></div>
+            </div>
+
+            <div class="cover-grid">
+              <article class="cover-card">
+                <div class="cover-card-title">Pacotes &amp; Entregas</div>
+                <div class="cover-card-meta">${data.items.length} blocos detalhados</div>
+                <div class="cover-card-detail">Serviços, entregas e diferenciais organizados por página.</div>
+                <div class="cover-card-page">Página ${refs.servicesStart}</div>
+              </article>
+
+              <article class="cover-card">
+                <div class="cover-card-title">Condições &amp; Formalização</div>
+                <div class="cover-card-meta" id="metricTotal">Investimento a calcular</div>
+                <div class="cover-card-detail">Objetivos, pagamento, prazo e aceite final do projeto.</div>
+                <div class="cover-card-page">Página ${refs.objectives}</div>
+              </article>
+            </div>
+          </div>
 
-      // Popular Objetivos (Apenas se preenchido)
-      const objectivesSection = document.getElementById('objectivesSection');
-      const objectivesContainer = document.getElementById('objectivesContainer');
-      objectivesContainer.innerHTML = '';
-      if (data.objectives && data.objectives.trim() !== '') {
-        const objLines = data.objectives.split('\n').filter(line => line.trim() !== '');
-        if (objLines.length > 0) {
-          objLines.forEach((line, idx) => {
-            const cardHtml = `
-              <div class="objective-card">
-                <div class="objective-num-wrapper">
-                  <span class="objective-num">${idx + 1}</span>
+          <div class="cover-footer-note">
+            <div class="sheet-foot-divider"></div>
+            <div>Preparado especialmente para <strong>${company}</strong>.</div>
+            <div>Aos cuidados de ${contact}. Cada seção abaixo traduz o escopo do projeto em páginas objetivas, comerciais e prontas para apresentação.</div>
+          </div>
+        </section>
+      `;
+    }
+
+    function buildScopeCard(item, globalIndex) {
+      const features = splitLines(item.desc);
+      const title = escapeHtml(item.title || `Serviço ${globalIndex + 1}`);
+      const price = escapeHtml(item.price || 'A combinar');
+      const subline = globalIndex === 0
+        ? 'Escopo base do projeto conforme briefing inicial.'
+        : 'Complemento estratégico e personalização sob medida conforme necessidade.';
+
+      const listHtml = features.length > 0
+        ? `<ul class="package-list">${features.map(line => `<li>${escapeHtml(line)}</li>`).join('')}</ul>`
+        : `<div class="notes-text">Escopo personalizado a ser detalhado entre as partes.</div>`;
+
+      return `
+        <article class="package-card scope-card" id="scopeCard_${globalIndex}">
+          <div class="package-header">
+            <div class="package-heading">
+              <input
+                type="checkbox"
+                checked
+                onchange="toggleScopeItem(${globalIndex})"
+                data-price="${escapeHtml(item.price || '')}"
+                class="package-checkbox scope-checkbox"
+                aria-label="Incluir ${title}"
+              >
+              <div class="package-title-wrap">
+                <div class="package-title scope-title">Pacote ${globalIndex + 1} — ${title}</div>
+                <div class="package-subline">${subline}</div>
+              </div>
+            </div>
+            <div class="package-price">${price}</div>
+          </div>
+          ${listHtml}
+        </article>
+      `;
+    }
+
+    function buildScopePages(groups, refs) {
+      return groups.map((group, groupIndex) => {
+        const sectionTitle = groupIndex === 0 ? 'Pacotes / Entregas' : `Pacotes Complementares ${groupIndex + 1}`;
+        const cards = group.map((item, itemIndex) => buildScopeCard(item, groupIndex * 3 + itemIndex)).join('');
+        return `
+          <section class="proposal-sheet">
+            <div class="sheet-section-bar">${escapeHtml(sectionTitle)}</div>
+            <div class="sheet-stack">${cards}</div>
+          <div class="sheet-contact-line">Estrutura comercial organizada em formato editorial para apresentação clara do escopo. Página ${refs.servicesStart + groupIndex}.</div>
+        </section>
+      `;
+      }).join('');
+    }
+
+    function buildObjectivesPage(data, refs) {
+      const intro = data.intro && data.intro.trim()
+        ? `<div class="notes-box"><div class="notes-title">Apresentação</div><div class="notes-text">${escapeHtml(data.intro)}</div></div>`
+        : '';
+
+      const objectives = splitLines(data.objectives);
+      const objectiveList = objectives.length
+        ? objectives.map(line => `<li>${escapeHtml(line)}</li>`).join('')
+        : '<li>Objetivos definidos de forma personalizada entre as partes.</li>';
+
+      return `
+        <section class="proposal-sheet">
+          <div class="sheet-section-bar">Observações do Projeto</div>
+          <div class="notes-layout">
+            ${intro}
+            <div class="notes-box">
+              <div class="notes-title">Objetivos Principais</div>
+              <ul class="notes-list">${objectiveList}</ul>
+            </div>
+            <div class="notes-columns">
+              <div class="notes-box">
+                <div class="notes-title">Cliente</div>
+                <div class="notes-text"><strong>${escapeHtml(data.company || 'Empresa Cliente')}</strong><br>${escapeHtml(data.contact || 'Contato principal')}</div>
+              </div>
+              <div class="notes-box">
+                <div class="notes-title">Mapa da Proposta</div>
+                <div class="notes-text">Escopo: página ${refs.servicesStart}<br>Comercial: página ${refs.commercial}<br>Formalização: página ${refs.signature}</div>
+              </div>
+            </div>
+          </div>
+          <div class="sheet-contact-line">Entre em contato para personalizar o pacote ideal e alinhar detalhes finais da operação.</div>
+        </section>
+      `;
+    }
+
+    function buildCommercialPage() {
+      return `
+        <section class="proposal-sheet">
+          <div class="sheet-section-bar">Condições Comerciais</div>
+          <div class="commercial-grid">
+            <article class="package-card">
+              <div class="package-header">
+                <div class="package-title-wrap">
+                  <div class="package-title">Investimento Global</div>
+                  <div class="package-subline">Soma dos escopos atualmente selecionados.</div>
                 </div>
-                <span class="objective-content">${line.trim()}</span>
+                <div class="package-price" id="financialTotal">R$ 0,00</div>
               </div>
-            `;
-            objectivesContainer.insertAdjacentHTML('beforeend', cardHtml);
-          });
-          objectivesSection.style.display = 'block';
-        } else {
-          objectivesSection.style.display = 'none';
-        }
-      } else {
-        objectivesSection.style.display = 'none';
-      }
-
-      // Popular Condições de Pagamento Detalhadas
-      const paymentContainer = document.getElementById('financialConditions');
-      const paymentLines = data.payment ? data.payment.split('\n').filter(line => line.trim() !== '') : [];
-      
-      if (paymentLines.length > 0) {
-        let paymentHtml = `<ul class="payment-conditions-list">`;
-        paymentLines.forEach(line => {
-          paymentHtml += `
-            <li>
-              <span class="payment-bullet"></span>
-              <span class="payment-item-text">${line.trim()}</span>
-            </li>
-          `;
-        });
-        paymentHtml += `</ul>`;
-        paymentContainer.innerHTML = paymentHtml;
-      } else {
-        paymentContainer.innerHTML = `<p class="payment-text">${data.payment || ''}</p>`;
-      }
+              <ul class="package-list">
+                <li>Os valores são atualizados conforme os blocos de escopo ativos nesta proposta.</li>
+                <li>O aceite formal considera apenas os itens selecionados no momento da aprovação.</li>
+              </ul>
+            </article>
+
+            <article class="package-card">
+              <div class="package-header">
+                <div class="package-title-wrap">
+                  <div class="package-title">Condições de Pagamento</div>
+                  <div class="package-subline">Condições comerciais definidas para este projeto.</div>
+                </div>
+              </div>
+              <div class="notes-text" id="financialConditions">Condições customizadas a serem carregadas dinamicamente.</div>
+            </article>
+
+            <article class="package-card">
+              <div class="package-header">
+                <div class="package-title-wrap">
+                  <div class="package-title">Prazo &amp; Validade</div>
+                  <div class="package-subline">Marco operacional para tomada de decisão.</div>
+                </div>
+              </div>
+              <ul class="package-list">
+                <li>Prazo estimado: <span id="metricDeadline">A definir</span></li>
+                <li>Validade da proposta: <span id="metricValidity">A definir</span></li>
+              </ul>
+            </article>
+
+            <div class="notes-box">
+              <div class="notes-title">Próximos Passos</div>
+              <ul class="notes-list">
+                <li>Revisar os blocos selecionados e confirmar o escopo ideal.</li>
+                <li>Assinar eletronicamente a proposta para liberar o PDF oficial.</li>
+                <li>Seguir para o WhatsApp da equipe DNA para formalização e kickoff.</li>
+              </ul>
+            </div>
+          </div>
+        </section>
+      `;
+    }
 
-      // Popular Lista de Escopos
-      const container = document.getElementById('scopeContainer');
-      container.innerHTML = ''; // Limpar default
+    function buildSignaturePage(data) {
+      return `
+        <section class="proposal-sheet signature-sheet">
+          <div>
+            <div class="sheet-section-bar">Formalização Final</div>
+            <div class="signature-sheet-main">
+              <div class="signature-copy">Esta última página concentra os dados essenciais para formalização do projeto. Após a assinatura, o PDF oficial será gerado exatamente nesta estrutura e liberado para registro.</div>
 
-      if (data.items && Array.isArray(data.items)) {
-        data.items.forEach((item, index) => {
-          const itemNum = String(index + 1).padStart(2, '0');
-          const priceBadge = item.price ? `<span class="scope-price-badge">${item.price}</span>` : '';
-          
-          // Split features by newline
-          const descLines = item.desc ? item.desc.split('\n').filter(line => line.trim() !== '') : [];
-          let descHtml = '';
-          
-          if (descLines.length > 0) {
-            descHtml = `<ul class="scope-features-list">`;
-            descLines.forEach(line => {
-              descHtml += `
-                <li>
-                  <svg class="feature-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
-                    <polyline points="20 6 9 17 4 12"></polyline>
-                  </svg>
-                  <span class="feature-text">${line.trim()}</span>
-                </li>
-              `;
-            });
-            descHtml += `</ul>`;
-          } else {
-            descHtml = `<p class="scope-description">${item.desc}</p>`;
-          }
-          
-          const itemHtml = `
-            <div class="scope-card" id="scopeCard_${index}">
-              <div class="scope-header">
-                <div class="scope-select-wrap">
-                  <label class="premium-checkbox-container">
-                    <input type="checkbox" checked onchange="toggleScopeItem(${index})" data-price="${item.price}" class="scope-checkbox">
-                    <span class="premium-checkbox-checkmark"></span>
-                  </label>
-                  <div class="scope-number-title">
-                    <span class="scope-num">SERVIÇO // ${itemNum}</span>
-                    <h3 class="scope-title">${item.title}</h3>
-                  </div>
+              <div class="signature-meta-grid">
+                <div class="signature-meta-card">
+                  <div class="signature-meta-label">Cliente</div>
+                  <div class="signature-meta-value" id="sigClientCompany">${escapeHtml(data.company || 'Empresa Cliente')}</div>
+                </div>
+                <div class="signature-meta-card">
+                  <div class="signature-meta-label">Contato Responsável</div>
+                  <div class="signature-meta-value">${escapeHtml(data.contact || 'Contato principal')}</div>
                 </div>
-                ${priceBadge}
               </div>
-              <div class="scope-body">
-                ${descHtml}
+
+              <div class="signature-section" id="signatureSection">
+                <h3 class="signature-title">Assinatura Eletrônica</h3>
+                <p class="signature-subtitle">Desenhe sua assinatura no quadro abaixo para validar a aprovação do escopo selecionado e gerar o documento oficial.</p>
+                <div class="signature-pad-container">
+                  <canvas id="signaturePad" width="500" height="200"></canvas>
+                </div>
+                <div class="signature-actions">
+                  <button type="button" class="clear-btn" id="clearSignatureBtn">Limpar Assinatura</button>
+                  <span style="color: #ef4444; font-size: 0.85rem; display: none; margin-top: 8px;" id="signatureError">Assinatura obrigatória</span>
+                </div>
+                <div class="print-signature-box" id="printSignatureBox">
+                  <img src="" id="printSignatureImg" class="print-signature-img" alt="Assinatura">
+                  <p class="signature-note" id="printSignatureName">Assinado eletronicamente via DNA Studio</p>
+                </div>
               </div>
             </div>
-          `;
-          container.insertAdjacentHTML('beforeend', itemHtml);
-        });
+          </div>
+
+          <div class="signature-footer-note">Entre em contato para personalizar o pacote ideal para você e alinhar o kickoff oficial do projeto.</div>
+        </section>
+      `;
+    }
+
+    function renderProposal(data) {
+      const items = Array.isArray(data.items) ? data.items : [];
+      const groups = items.length > 0 ? chunkItems(items, 3) : chunkItems([{ title: 'Escopo personalizado', price: 'A combinar', desc: 'Escopo a ser definido entre as partes' }], 3);
+      const refs = {
+        servicesStart: 2,
+        objectives: 2 + groups.length,
+        commercial: 3 + groups.length,
+        signature: 4 + groups.length
+      };
+
+      document.getElementById('proposalPages').innerHTML = [
+        buildCoverPage({ ...data, items }, refs),
+        buildScopePages(groups, refs),
+        buildObjectivesPage(data, refs),
+        buildCommercialPage(),
+        buildSignaturePage(data)
+      ].join('');
+
+      const paymentContainer = document.getElementById('financialConditions');
+      const paymentLines = splitLines(data.payment);
+      if (paymentLines.length > 0) {
+        paymentContainer.innerHTML = `<ul class="notes-list">${paymentLines.map(line => `<li>${escapeHtml(line)}</li>`).join('')}</ul>`;
+      } else {
+        paymentContainer.innerHTML = `<div class="notes-text">${escapeHtml(data.payment || 'Condições comerciais definidas sob medida entre as partes.')}</div>`;
       }
 
-      // Realizar o primeiro cálculo de investimento com base nas seleções iniciais
-      calculateScopeTotals();
+      const deadlineEl = document.getElementById('metricDeadline');
+      const validityEl = document.getElementById('metricValidity');
+      if (deadlineEl) deadlineEl.textContent = data.deadline || 'A definir';
+      if (validityEl) validityEl.textContent = data.validity || 'A definir';
 
-      // Exibir página da Proposta
-      document.getElementById('proposalView').style.display = 'block';
-    }
+      const proposalView = document.getElementById('proposalView');
+      proposalView.style.display = 'block';
 
-    // --- FUNÇÕES DE CÁLCULO E INTERATIVIDADE (ITERAÇÃO 3) ---
+      requestAnimationFrame(() => {
+        initializeInteractiveProposal();
+        calculateScopeTotals();
+        updateWhatsAppCTAWithFallback();
+      });
+    }
 
     function toggleScopeItem(index) {
       const card = document.getElementById(`scopeCard_${index}`);
       if (!card) return;
-      
-      const cb = card.querySelector('.scope-checkbox');
-      if (cb) {
-        if (cb.checked) {
-          card.classList.remove('is-deselected');
-        } else {
-          card.classList.add('is-deselected');
-        }
+
+      const checkbox = card.querySelector('.scope-checkbox');
+      if (checkbox) {
+        card.classList.toggle('is-deselected', !checkbox.checked);
       }
-      
+
       calculateScopeTotals();
     }
 
     function parsePriceString(priceStr) {
       if (!priceStr) return 0;
+
       let cleaned = priceStr.toLowerCase().replace(/\s/g, '');
       if (!/\d/.test(cleaned)) {
         return 0;
       }
+
       cleaned = cleaned.replace(/r\$/g, '');
-      
       const matches = cleaned.match(/[0-9.,]+/);
       if (!matches) return 0;
-      
+
       let numStr = matches[0];
       const hasComma = numStr.includes(',');
       const hasDot = numStr.includes('.');
-      
+
       if (hasComma && hasDot) {
         numStr = numStr.replace(/\./g, '').replace(/,/g, '.');
       } else if (hasComma) {
         numStr = numStr.replace(/,/g, '.');
       } else if (hasDot) {
         const parts = numStr.split('.');
-        const lastPart = parts[parts.length - 1];
-        if (lastPart.length === 3 && parts.length === 2) {
+        if (parts.length === 2 && parts[1].length === 3) {
           numStr = numStr.replace(/\./g, '');
         }
       }
-      
+
       const parsed = parseFloat(numStr);
-      return isNaN(parsed) ? 0 : parsed;
+      return Number.isNaN(parsed) ? 0 : parsed;
     }
 
     function isMonthlyPrice(priceStr) {
@@ -1722,43 +2019,153 @@
       return lower.includes('mês') || lower.includes('mensal') || lower.includes('/m') || lower.includes('recorrente');
     }
 
-    function calculateScopeTotals() {
-      const urlParams = new URLSearchParams(window.location.search);
-      const dataParam = urlParams.get('d');
-      const decodedJson = b64_to_utf8(dataParam);
-      if (!decodedJson) return;
-      const data = JSON.parse(decodedJson);
+    function formatCurrency(value) {
+      return value.toLocaleString('pt-BR', {
+        style: 'currency',
+        currency: 'BRL',
+        minimumFractionDigits: 2
+      });
+    }
+
+    function createFallbackSignaturePad(canvas) {
+      const context = canvas.getContext('2d');
+      let isEmpty = true;
+      let isDrawing = false;
+      let lastPoint = null;
+      let activePointerId = null;
 
+      function configureContext() {
+        context.lineCap = 'round';
+        context.lineJoin = 'round';
+        context.lineWidth = 2.4;
+        context.strokeStyle = 'rgb(10, 22, 40)';
+      }
+
+      function getPoint(event) {
+        const rect = canvas.getBoundingClientRect();
+        return {
+          x: event.clientX - rect.left,
+          y: event.clientY - rect.top
+        };
+      }
+
+      function startStroke(event) {
+        if (event.button !== undefined && event.button !== 0) return;
+
+        activePointerId = event.pointerId ?? null;
+        isDrawing = true;
+        lastPoint = getPoint(event);
+        configureContext();
+        context.beginPath();
+        context.moveTo(lastPoint.x, lastPoint.y);
+        context.lineTo(lastPoint.x + 0.01, lastPoint.y + 0.01);
+        context.stroke();
+        isEmpty = false;
+
+        if (canvas.setPointerCapture && event.pointerId !== undefined) {
+          canvas.setPointerCapture(event.pointerId);
+        }
+
+        event.preventDefault();
+      }
+
+      function moveStroke(event) {
+        if (!isDrawing) return;
+        if (activePointerId !== null && event.pointerId !== undefined && event.pointerId !== activePointerId) {
+          return;
+        }
+
+        const point = getPoint(event);
+        configureContext();
+        context.beginPath();
+        context.moveTo(lastPoint.x, lastPoint.y);
+        context.lineTo(point.x, point.y);
+        context.stroke();
+        lastPoint = point;
+        isEmpty = false;
+        event.preventDefault();
+      }
+
+      function endStroke(event) {
+        if (activePointerId !== null && event?.pointerId !== undefined && event.pointerId !== activePointerId) {
+          return;
+        }
+
+        isDrawing = false;
+        lastPoint = null;
+        activePointerId = null;
+      }
+
+      canvas.addEventListener('pointerdown', startStroke);
+      canvas.addEventListener('pointermove', moveStroke);
+      canvas.addEventListener('pointerup', endStroke);
+      canvas.addEventListener('pointerleave', endStroke);
+      canvas.addEventListener('pointercancel', endStroke);
+
+      configureContext();
+
+      return {
+        clear() {
+          context.clearRect(0, 0, canvas.width, canvas.height);
+          configureContext();
+          isEmpty = true;
+        },
+        isEmpty() {
+          return isEmpty;
+        },
+        toDataURL(type = 'image/png') {
+          return canvas.toDataURL(type);
+        },
+        fromDataURL(dataUrl) {
+          return new Promise(resolve => {
+            const image = new Image();
+            image.onload = () => {
+              context.clearRect(0, 0, canvas.width, canvas.height);
+              context.drawImage(image, 0, 0, canvas.width, canvas.height);
+              configureContext();
+              isEmpty = false;
+              resolve();
+            };
+            image.onerror = () => resolve();
+            image.src = dataUrl;
+          });
+        },
+        off() {
+          canvas.removeEventListener('pointerdown', startStroke);
+          canvas.removeEventListener('pointermove', moveStroke);
+          canvas.removeEventListener('pointerup', endStroke);
+          canvas.removeEventListener('pointerleave', endStroke);
+          canvas.removeEventListener('pointercancel', endStroke);
+        }
+      };
+    }
+
+    function calculateScopeTotals() {
       let oneOffTotal = 0;
       let monthlyTotal = 0;
-      
-      const checkboxes = document.querySelectorAll('.scope-checkbox');
-      checkboxes.forEach(cb => {
-        if (cb.checked) {
-          const priceStr = cb.getAttribute('data-price');
-          const value = parsePriceString(priceStr);
-          if (isMonthlyPrice(priceStr)) {
-            monthlyTotal += value;
-          } else {
-            oneOffTotal += value;
-          }
+
+      document.querySelectorAll('.scope-checkbox').forEach(checkbox => {
+        if (!checkbox.checked) return;
+
+        const priceStr = checkbox.getAttribute('data-price');
+        const value = parsePriceString(priceStr);
+        if (isMonthlyPrice(priceStr)) {
+          monthlyTotal += value;
+        } else {
+          oneOffTotal += value;
         }
       });
-      
-      const formatCurrency = (val) => {
-        return val.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL', minimumFractionDigits: 2 });
-      };
-      
+
       const totalBlock = document.getElementById('financialTotal');
       const headerTotalBlock = document.getElementById('metricTotal');
-      
+      const acceptBtn = document.getElementById('acceptBtn');
+
       let totalHtml = '';
       let simpleText = '';
-      
-      const acceptBtn = document.getElementById('acceptBtn');
+
       if (oneOffTotal === 0 && monthlyTotal === 0) {
-        totalHtml = `R$ 0,00 <span>*</span>`;
-        simpleText = `Nenhum serviço selecionado`;
+        totalHtml = 'R$ 0,00 <span>*</span>';
+        simpleText = 'Nenhum serviço selecionado';
         if (acceptBtn) {
           acceptBtn.classList.add('disabled');
         }
@@ -1766,153 +2173,323 @@
         if (acceptBtn) {
           acceptBtn.classList.remove('disabled');
         }
+
         if (oneOffTotal > 0 && monthlyTotal > 0) {
           totalHtml = `${formatCurrency(oneOffTotal)} <span>(Único)</span> + ${formatCurrency(monthlyTotal)}<span>/mês</span>`;
           simpleText = `${formatCurrency(oneOffTotal)} + ${formatCurrency(monthlyTotal)}/mês`;
         } else if (oneOffTotal > 0) {
           totalHtml = `${formatCurrency(oneOffTotal)} <span>(Único)</span>`;
-          simpleText = `${formatCurrency(oneOffTotal)}`;
-        } else if (monthlyTotal > 0) {
+          simpleText = formatCurrency(oneOffTotal);
+        } else {
           totalHtml = `${formatCurrency(monthlyTotal)}<span>/mês</span>`;
           simpleText = `${formatCurrency(monthlyTotal)}/mês`;
         }
-        
+
         updateWhatsAppCTADynamic(oneOffTotal, monthlyTotal);
       }
-      
+
       if (totalBlock) totalBlock.innerHTML = totalHtml;
       if (headerTotalBlock) headerTotalBlock.textContent = simpleText;
     }
 
-    function updateWhatsAppCTAWithFallback(rawTotal) {
-      const urlParams = new URLSearchParams(window.location.search);
-      const dataParam = urlParams.get('d');
-      const decodedJson = b64_to_utf8(dataParam);
-      if (!decodedJson) return;
-      const data = JSON.parse(decodedJson);
+    function updateWhatsAppCTAWithFallback() {
+      const data = getProposalDataFromUrl();
+      if (!data) return;
 
       const whatsappNumber = data.whatsapp || '5513991863669';
       const companyName = data.company || '';
       const message = `Olá! Acabei de visualizar a proposta comercial da DNA Studio para o projeto da ${companyName} e gostaria de dar o aceite oficial para darmos início!`;
-      
-      const acceptBtn = document.getElementById('acceptBtn');
-      if (acceptBtn) {
-        window.currentWhatsappUrl = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(message)}`;
-      }
+      window.currentWhatsappUrl = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(message)}`;
     }
 
     function updateWhatsAppCTADynamic(oneOff, monthly) {
-      const urlParams = new URLSearchParams(window.location.search);
-      const dataParam = urlParams.get('d');
-      const decodedJson = b64_to_utf8(dataParam);
-      if (!decodedJson) return;
-      const data = JSON.parse(decodedJson);
+      const data = getProposalDataFromUrl();
+      if (!data) return;
 
       const whatsappNumber = data.whatsapp || '5513991863669';
       const companyName = data.company || '';
-      
       const checkedCards = [];
-      const checkboxes = document.querySelectorAll('.scope-checkbox');
-      checkboxes.forEach(cb => {
-        if (cb.checked) {
-          const card = cb.closest('.scope-card');
-          const title = card.querySelector('.scope-title').textContent;
+
+      document.querySelectorAll('.scope-checkbox').forEach(checkbox => {
+        if (!checkbox.checked) return;
+
+        const card = checkbox.closest('.scope-card');
+        const title = card?.querySelector('.scope-title')?.textContent;
+        if (title) {
           checkedCards.push(title);
         }
       });
-      
-      const servicesList = checkedCards.map(s => `• ${s}`).join('\n');
-      
+
+      const servicesList = checkedCards.map(service => `• ${service}`).join('\n');
+
       let priceText = '';
       if (oneOff > 0 && monthly > 0) {
-        priceText = `R$ ${oneOff.toLocaleString('pt-BR')} (Único) + R$ ${monthly.toLocaleString('pt-BR')}/mês`;
+        priceText = `${formatCurrency(oneOff)} (Único) + ${formatCurrency(monthly)}/mês`;
       } else if (oneOff > 0) {
-        priceText = `R$ ${oneOff.toLocaleString('pt-BR')} (Único)`;
+        priceText = `${formatCurrency(oneOff)} (Único)`;
       } else if (monthly > 0) {
-        priceText = `R$ ${monthly.toLocaleString('pt-BR')}/mês`;
+        priceText = `${formatCurrency(monthly)}/mês`;
       }
-      
+
       const message = `Olá! Acabei de aprovar a proposta da DNA Studio para o projeto da ${companyName}!\n\n*Escopo Selecionado:*\n${servicesList}\n\n*Investimento:* ${priceText}\n\nGostaria de dar o aceite oficial e prosseguir!`;
-      
-      const acceptBtn = document.getElementById('acceptBtn');
-      if (acceptBtn) {
-        window.currentWhatsappUrl = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(message)}`;
-      }
+      window.currentWhatsappUrl = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(message)}`;
     }
 
     let signaturePad;
-    
-    document.addEventListener("DOMContentLoaded", () => {
+    let signatureResizeHandler;
+    let signaturePadCleanup;
+    let isGeneratingPdf = false;
+
+    function initializeInteractiveProposal() {
       const canvas = document.getElementById('signaturePad');
-      if (canvas && window.SignaturePad) {
-        function resizeCanvas() {
-          const ratio = Math.max(window.devicePixelRatio || 1, 1);
-          canvas.width = canvas.offsetWidth * ratio;
-          canvas.height = canvas.offsetHeight * ratio;
-          canvas.getContext("2d").scale(ratio, ratio);
-          if (signaturePad) {
-            signaturePad.clear();
-          }
+      const clearBtn = document.getElementById('clearSignatureBtn');
+      const acceptBtn = document.getElementById('acceptBtn');
+      const signatureError = document.getElementById('signatureError');
+      const signatureName = document.getElementById('printSignatureName');
+      const data = getProposalDataFromUrl() || {};
+
+      if (signatureName) {
+        const signerName = data.contact || data.company || 'Cliente';
+        signatureName.textContent = `Assinado eletronicamente por ${signerName} via DNA Studio`;
+      }
+
+      if (acceptBtn) {
+        acceptBtn.href = '#';
+        acceptBtn.target = '';
+        acceptBtn.removeEventListener('click', handleAcceptAndPDF);
+        acceptBtn.addEventListener('click', handleAcceptAndPDF);
+      }
+
+      if (!canvas) {
+        return;
+      }
+
+      if (signatureResizeHandler) {
+        window.removeEventListener('resize', signatureResizeHandler);
+      }
+
+      signatureResizeHandler = () => {
+        const ratio = Math.max(window.devicePixelRatio || 1, 1);
+        const rect = canvas.getBoundingClientRect();
+        if (rect.width < 2 || rect.height < 2) {
+          requestAnimationFrame(signatureResizeHandler);
+          return;
         }
-        window.addEventListener("resize", resizeCanvas);
-        resizeCanvas();
-
-        signaturePad = new SignaturePad(canvas, {
-          penColor: "rgb(0, 0, 0)",
-          backgroundColor: "rgb(255, 255, 255)"
-        });
-        
-        document.getElementById('clearSignatureBtn').addEventListener('click', () => {
+
+        const currentSignature = signaturePad && !signaturePad.isEmpty()
+          ? signaturePad.toDataURL('image/png')
+          : null;
+
+        canvas.width = Math.max(Math.floor(rect.width * ratio), 1);
+        canvas.height = Math.max(Math.floor(rect.height * ratio), 1);
+        const context = canvas.getContext('2d');
+        context.setTransform(1, 0, 0, 1, 0, 0);
+        context.scale(ratio, ratio);
+
+        if (signaturePadCleanup) {
+          signaturePadCleanup();
+          signaturePadCleanup = null;
+        }
+
+        if (window.SignaturePad) {
+          signaturePad = new SignaturePad(canvas, {
+            penColor: 'rgb(10, 22, 40)',
+            backgroundColor: 'rgb(255, 255, 255)'
+          });
+          signaturePadCleanup = () => signaturePad?.off?.();
+        } else {
+          signaturePad = createFallbackSignaturePad(canvas);
+          signaturePadCleanup = () => signaturePad?.off?.();
+        }
+
+        if (currentSignature) {
+          signaturePad.fromDataURL(currentSignature);
+        }
+      };
+
+      window.addEventListener('resize', signatureResizeHandler);
+      requestAnimationFrame(signatureResizeHandler);
+
+      canvas.onpointerdown = () => {
+        if (signatureError) {
+          signatureError.style.display = 'none';
+        }
+        setSignaturePreviewState(false);
+      };
+
+      if (clearBtn) {
+        clearBtn.onclick = () => {
           signaturePad.clear();
-          document.getElementById('signatureError').style.display = 'none';
-        });
+          if (signatureError) {
+            signatureError.style.display = 'none';
+          }
+          setSignaturePreviewState(false);
+        };
       }
-      
+    }
+
+    function slugifyFilePart(value) {
+      return String(value || '')
+        .normalize('NFD')
+        .replace(/[\u0300-\u036f]/g, '')
+        .toLowerCase()
+        .replace(/[^a-z0-9]+/g, '-')
+        .replace(/^-+|-+$/g, '')
+        .slice(0, 50);
+    }
+
+    function buildProposalPdfFilename() {
+      const data = getProposalDataFromUrl() || {};
+      const company = slugifyFilePart(data.company);
+      const title = slugifyFilePart(data.title);
+      const suffix = [company, title].filter(Boolean).join('-');
+      return suffix ? `proposta-dna-${suffix}.pdf` : 'proposta-dna-studio.pdf';
+    }
+
+    function setSignaturePreviewState(visible) {
+      const signaturePadContainer = document.querySelector('.signature-pad-container');
+      const signatureActions = document.querySelector('.signature-actions');
+      const printSignatureBox = document.getElementById('printSignatureBox');
+      const printSignatureImg = document.getElementById('printSignatureImg');
+
+      if (!signaturePadContainer || !signatureActions || !printSignatureBox) return;
+
+      if (visible && signaturePad && !signaturePad.isEmpty()) {
+        if (printSignatureImg) {
+          printSignatureImg.src = signaturePad.toDataURL('image/png');
+        }
+        signaturePadContainer.style.display = 'none';
+        signatureActions.style.display = 'none';
+        printSignatureBox.style.display = 'flex';
+        return;
+      }
+
+      signaturePadContainer.style.display = '';
+      signatureActions.style.display = 'flex';
+      printSignatureBox.style.display = 'none';
+    }
+
+    async function exportProposalPdf({ requireSignature = false, persistSignaturePreview = false } = {}) {
+      if (isGeneratingPdf) {
+        return false;
+      }
+
+      const proposalView = document.getElementById('proposalView');
+      const proposalPages = document.getElementById('proposalPages');
+      if (!proposalView || !proposalPages) {
+        return false;
+      }
+
+      if (requireSignature && (!signaturePad || signaturePad.isEmpty())) {
+        document.getElementById('signatureError').style.display = 'inline-block';
+        document.getElementById('signatureSection').scrollIntoView({ behavior: 'smooth' });
+        return false;
+      }
+
+      const hasSignature = Boolean(signaturePad && !signaturePad.isEmpty());
+      setSignaturePreviewState(hasSignature);
+
+      isGeneratingPdf = true;
+      document.body.classList.add('pdf-exporting');
+
+      try {
+        if (document.fonts?.ready) {
+          await document.fonts.ready;
+        }
+
+        if (window.html2pdf) {
+          await window.html2pdf()
+            .set({
+              margin: 0,
+              filename: buildProposalPdfFilename(),
+              image: { type: 'jpeg', quality: 0.98 },
+              html2canvas: {
+                scale: 2,
+                useCORS: true,
+                backgroundColor: '#ffffff',
+                scrollX: 0,
+                scrollY: 0,
+                windowWidth: proposalPages.scrollWidth
+              },
+              jsPDF: { unit: 'mm', format: 'a4', orientation: 'portrait' },
+              pagebreak: { mode: ['css', 'legacy'] }
+            })
+            .from(proposalPages)
+            .save();
+        } else {
+          window.print();
+        }
+
+        return true;
+      } catch (err) {
+        console.error('Falha ao gerar PDF', err);
+        return false;
+      } finally {
+        document.body.classList.remove('pdf-exporting');
+        isGeneratingPdf = false;
+
+        if (persistSignaturePreview && hasSignature) {
+          setSignaturePreviewState(true);
+        } else {
+          setSignaturePreviewState(false);
+        }
+      }
+    }
+
+    async function downloadProposalPdf() {
+      await exportProposalPdf();
+    }
+
+    async function handleAcceptAndPDF(event) {
+      event.preventDefault();
+
       const acceptBtn = document.getElementById('acceptBtn');
-      if (acceptBtn) {
-        acceptBtn.addEventListener('click', handleAcceptAndPDF);
+      const acceptBtnText = document.getElementById('acceptBtnText');
+      const acceptBtnIcon = document.getElementById('acceptBtnIcon');
+
+      if (!acceptBtn || acceptBtn.classList.contains('disabled')) {
+        return;
       }
-    });
 
-    async function handleAcceptAndPDF(e) {
-      e.preventDefault();
-      
-      if (document.getElementById('acceptBtn').classList.contains('disabled')) return;
-      
-      if (signaturePad && signaturePad.isEmpty()) {
+      if (!signaturePad || signaturePad.isEmpty()) {
         document.getElementById('signatureError').style.display = 'inline-block';
         document.getElementById('signatureSection').scrollIntoView({ behavior: 'smooth' });
         return;
       }
-      
+
       document.getElementById('signatureError').style.display = 'none';
-      
-      if (signaturePad) {
-        const dataUrl = signaturePad.toDataURL();
-        document.getElementById('printSignatureImg').src = dataUrl;
-        document.getElementById('signaturePad').parentElement.style.display = 'none';
-        document.getElementById('clearSignatureBtn').style.display = 'none';
-        document.getElementById('printSignatureBox').style.display = 'block';
+      acceptBtnText.textContent = 'Preparando PDF...';
+      if (acceptBtnIcon) {
+        acceptBtnIcon.style.display = 'none';
       }
-      
-      document.getElementById('acceptBtnText').innerHTML = 'Preparando Documento...';
-      if(document.getElementById('acceptBtnIcon')) document.getElementById('acceptBtnIcon').style.display = 'none';
-      
-      setTimeout(() => {
-        window.print();
-        
-        document.getElementById('acceptBtnText').innerHTML = 'Avançar para o WhatsApp';
-        if(document.getElementById('acceptBtnIcon')) {
-          document.getElementById('acceptBtnIcon').style.display = 'inline-block';
-        }
-        
-        const acceptBtn = document.getElementById('acceptBtn');
-        if (acceptBtn && window.currentWhatsappUrl) {
-          acceptBtn.href = window.currentWhatsappUrl;
-          acceptBtn.target = '_blank';
-          acceptBtn.removeEventListener('click', handleAcceptAndPDF);
+      acceptBtn.style.pointerEvents = 'none';
+
+      const success = await exportProposalPdf({
+        requireSignature: true,
+        persistSignaturePreview: true
+      });
+
+      if (!success) {
+        acceptBtnText.innerHTML = 'Aceitar Proposta &amp; Iniciar Projeto';
+        if (acceptBtnIcon) {
+          acceptBtnIcon.style.display = 'inline-block';
         }
-      }, 500);
+        acceptBtn.style.pointerEvents = '';
+        return;
+      }
+
+      acceptBtnText.textContent = 'Avançar para o WhatsApp';
+      if (acceptBtnIcon) {
+        acceptBtnIcon.style.display = 'inline-block';
+      }
+
+      if (window.currentWhatsappUrl) {
+        acceptBtn.href = window.currentWhatsappUrl;
+        acceptBtn.target = '_blank';
+        acceptBtn.removeEventListener('click', handleAcceptAndPDF);
+      }
+
+      acceptBtn.style.pointerEvents = '';
     }
   </script>
 </body>
